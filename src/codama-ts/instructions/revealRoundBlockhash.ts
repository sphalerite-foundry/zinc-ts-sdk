/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBoardPda, findConfigPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const REVEAL_ROUND_BLOCKHASH_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([204, 143, 123, 117, 220, 68, 251, 118]);

export function getRevealRoundBlockhashDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVEAL_ROUND_BLOCKHASH_DISCRIMINATOR,
  );
}

export type RevealRoundBlockhashInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountSlotHashes extends string | AccountMeta<string> =
    "SysvarS1otHashes111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBoard extends string
        ? WritableAccount<TAccountBoard>
        : TAccountBoard,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountSlotHashes extends string
        ? ReadonlyAccount<TAccountSlotHashes>
        : TAccountSlotHashes,
      ...TRemainingAccounts,
    ]
  >;

export type RevealRoundBlockhashInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevealRoundBlockhashInstructionDataArgs = {};

export function getRevealRoundBlockhashInstructionDataEncoder(): FixedSizeEncoder<RevealRoundBlockhashInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REVEAL_ROUND_BLOCKHASH_DISCRIMINATOR,
    }),
  );
}

export function getRevealRoundBlockhashInstructionDataDecoder(): FixedSizeDecoder<RevealRoundBlockhashInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevealRoundBlockhashInstructionDataCodec(): FixedSizeCodec<
  RevealRoundBlockhashInstructionDataArgs,
  RevealRoundBlockhashInstructionData
> {
  return combineCodec(
    getRevealRoundBlockhashInstructionDataEncoder(),
    getRevealRoundBlockhashInstructionDataDecoder(),
  );
}

export type RevealRoundBlockhashAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountSlotHashes extends string = string,
> = {
  /** Crank authorized to reveal a closed round through the configured blockhash path. */
  signer: TransactionSigner<TAccountSigner>;
  /** Live config selecting the accepted reveal mode and delay. */
  config?: Address<TAccountConfig>;
  /** Board transition gate opened by this reveal. */
  board?: Address<TAccountBoard>;
  /** Closed round waiting for blockhash reveal. */
  round: Address<TAccountRound>;
  slotHashes?: Address<TAccountSlotHashes>;
};

export async function getRevealRoundBlockhashInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountSlotHashes extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: RevealRoundBlockhashAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountSlotHashes
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevealRoundBlockhashInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountSlotHashes
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    slotHashes: { value: input.slotHashes ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }
  if (!accounts.slotHashes.value) {
    accounts.slotHashes.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("slotHashes", accounts.slotHashes),
    ],
    data: getRevealRoundBlockhashInstructionDataEncoder().encode({}),
    programAddress,
  } as RevealRoundBlockhashInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountSlotHashes
  >);
}

export type RevealRoundBlockhashInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountSlotHashes extends string = string,
> = {
  /** Crank authorized to reveal a closed round through the configured blockhash path. */
  signer: TransactionSigner<TAccountSigner>;
  /** Live config selecting the accepted reveal mode and delay. */
  config: Address<TAccountConfig>;
  /** Board transition gate opened by this reveal. */
  board: Address<TAccountBoard>;
  /** Closed round waiting for blockhash reveal. */
  round: Address<TAccountRound>;
  slotHashes?: Address<TAccountSlotHashes>;
};

export function getRevealRoundBlockhashInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountSlotHashes extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: RevealRoundBlockhashInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountSlotHashes
  >,
  config?: { programAddress?: TProgramAddress },
): RevealRoundBlockhashInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountBoard,
  TAccountRound,
  TAccountSlotHashes
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    slotHashes: { value: input.slotHashes ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.slotHashes.value) {
    accounts.slotHashes.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("slotHashes", accounts.slotHashes),
    ],
    data: getRevealRoundBlockhashInstructionDataEncoder().encode({}),
    programAddress,
  } as RevealRoundBlockhashInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountSlotHashes
  >);
}

export type ParsedRevealRoundBlockhashInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank authorized to reveal a closed round through the configured blockhash path. */
    signer: TAccountMetas[0];
    /** Live config selecting the accepted reveal mode and delay. */
    config: TAccountMetas[1];
    /** Board transition gate opened by this reveal. */
    board: TAccountMetas[2];
    /** Closed round waiting for blockhash reveal. */
    round: TAccountMetas[3];
    slotHashes: TAccountMetas[4];
  };
  data: RevealRoundBlockhashInstructionData;
};

export function parseRevealRoundBlockhashInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevealRoundBlockhashInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      board: getNextAccount(),
      round: getNextAccount(),
      slotHashes: getNextAccount(),
    },
    data: getRevealRoundBlockhashInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
