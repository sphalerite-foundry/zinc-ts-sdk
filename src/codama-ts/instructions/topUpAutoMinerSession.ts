/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAutoMinerSessionPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const TOP_UP_AUTO_MINER_SESSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([130, 226, 97, 3, 111, 130, 14, 222]);

export function getTopUpAutoMinerSessionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TOP_UP_AUTO_MINER_SESSION_DISCRIMINATOR,
  );
}

export type TopUpAutoMinerSessionInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAutoMinerSession extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAutoMinerSession extends string
        ? WritableAccount<TAccountAutoMinerSession>
        : TAccountAutoMinerSession,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TopUpAutoMinerSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** Lamports to add to the spendable auto-miner budget. */
  amount: bigint;
};

export type TopUpAutoMinerSessionInstructionDataArgs = {
  /** Lamports to add to the spendable auto-miner budget. */
  amount: number | bigint;
};

export function getTopUpAutoMinerSessionInstructionDataEncoder(): FixedSizeEncoder<TopUpAutoMinerSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: TOP_UP_AUTO_MINER_SESSION_DISCRIMINATOR,
    }),
  );
}

export function getTopUpAutoMinerSessionInstructionDataDecoder(): FixedSizeDecoder<TopUpAutoMinerSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getTopUpAutoMinerSessionInstructionDataCodec(): FixedSizeCodec<
  TopUpAutoMinerSessionInstructionDataArgs,
  TopUpAutoMinerSessionInstructionData
> {
  return combineCodec(
    getTopUpAutoMinerSessionInstructionDataEncoder(),
    getTopUpAutoMinerSessionInstructionDataDecoder(),
  );
}

export type TopUpAutoMinerSessionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet that owns and funds the session. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Player-owned auto-miner session PDA. */
  autoMinerSession?: Address<TAccountAutoMinerSession>;
  /** System program used for the budget transfer. */
  systemProgram?: Address<TAccountSystemProgram>;
  amount: TopUpAutoMinerSessionInstructionDataArgs["amount"];
};

export async function getTopUpAutoMinerSessionInstructionAsync<
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: TopUpAutoMinerSessionAsyncInput<
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TopUpAutoMinerSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.autoMinerSession.value) {
    accounts.autoMinerSession.value = await findAutoMinerSessionPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getTopUpAutoMinerSessionInstructionDataEncoder().encode(
      args as TopUpAutoMinerSessionInstructionDataArgs,
    ),
    programAddress,
  } as TopUpAutoMinerSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountSystemProgram
  >);
}

export type TopUpAutoMinerSessionInput<
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet that owns and funds the session. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Player-owned auto-miner session PDA. */
  autoMinerSession: Address<TAccountAutoMinerSession>;
  /** System program used for the budget transfer. */
  systemProgram?: Address<TAccountSystemProgram>;
  amount: TopUpAutoMinerSessionInstructionDataArgs["amount"];
};

export function getTopUpAutoMinerSessionInstruction<
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: TopUpAutoMinerSessionInput<
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): TopUpAutoMinerSessionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAutoMinerSession,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getTopUpAutoMinerSessionInstructionDataEncoder().encode(
      args as TopUpAutoMinerSessionInstructionDataArgs,
    ),
    programAddress,
  } as TopUpAutoMinerSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountSystemProgram
  >);
}

export type ParsedTopUpAutoMinerSessionInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet that owns and funds the session. */
    authority: TAccountMetas[0];
    /** Player-owned auto-miner session PDA. */
    autoMinerSession: TAccountMetas[1];
    /** System program used for the budget transfer. */
    systemProgram: TAccountMetas[2];
  };
  data: TopUpAutoMinerSessionInstructionData;
};

export function parseTopUpAutoMinerSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTopUpAutoMinerSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      autoMinerSession: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getTopUpAutoMinerSessionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
