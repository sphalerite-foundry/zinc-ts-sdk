/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findBoardPda,
  findConfigPda,
  findStockpileExtrasPda,
  findTreasuryPda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const PAYOUT_STOCKPILE_EXTRA_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([183, 36, 21, 147, 186, 150, 224, 246]);

export function getPayoutStockpileExtraDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAYOUT_STOCKPILE_EXTRA_DISCRIMINATOR,
  );
}

export type PayoutStockpileExtraInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountStockpile extends string | AccountMeta<string> = string,
  TAccountStockpileExtras extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountExtraMint extends string | AccountMeta<string> = string,
  TAccountStockpileExtraTokenAccount extends string | AccountMeta<string> =
    string,
  TAccountWinner extends string | AccountMeta<string> = string,
  TAccountWinnerExtraTokenAccount extends string | AccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountStockpile extends string
        ? WritableAccount<TAccountStockpile>
        : TAccountStockpile,
      TAccountStockpileExtras extends string
        ? WritableAccount<TAccountStockpileExtras>
        : TAccountStockpileExtras,
      TAccountBoard extends string
        ? WritableAccount<TAccountBoard>
        : TAccountBoard,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountExtraMint extends string
        ? ReadonlyAccount<TAccountExtraMint>
        : TAccountExtraMint,
      TAccountStockpileExtraTokenAccount extends string
        ? WritableAccount<TAccountStockpileExtraTokenAccount>
        : TAccountStockpileExtraTokenAccount,
      TAccountWinner extends string
        ? WritableAccount<TAccountWinner>
        : TAccountWinner,
      TAccountWinnerExtraTokenAccount extends string
        ? WritableAccount<TAccountWinnerExtraTokenAccount>
        : TAccountWinnerExtraTokenAccount,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PayoutStockpileExtraInstructionData = {
  discriminator: ReadonlyUint8Array;
  extraIndex: number;
};

export type PayoutStockpileExtraInstructionDataArgs = { extraIndex: number };

export function getPayoutStockpileExtraInstructionDataEncoder(): FixedSizeEncoder<PayoutStockpileExtraInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["extraIndex", getU16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PAYOUT_STOCKPILE_EXTRA_DISCRIMINATOR,
    }),
  );
}

export function getPayoutStockpileExtraInstructionDataDecoder(): FixedSizeDecoder<PayoutStockpileExtraInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["extraIndex", getU16Decoder()],
  ]);
}

export function getPayoutStockpileExtraInstructionDataCodec(): FixedSizeCodec<
  PayoutStockpileExtraInstructionDataArgs,
  PayoutStockpileExtraInstructionData
> {
  return combineCodec(
    getPayoutStockpileExtraInstructionDataEncoder(),
    getPayoutStockpileExtraInstructionDataDecoder(),
  );
}

export type PayoutStockpileExtraAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountStockpile extends string = string,
  TAccountStockpileExtras extends string = string,
  TAccountBoard extends string = string,
  TAccountTreasury extends string = string,
  TAccountExtraMint extends string = string,
  TAccountStockpileExtraTokenAccount extends string = string,
  TAccountWinner extends string = string,
  TAccountWinnerExtraTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Crank signer that executes one extra-prize payout. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority. */
  config?: Address<TAccountConfig>;
  /** Stockpile whose extra-prize payout is being processed. */
  stockpile: Address<TAccountStockpile>;
  /** Singleton pending-extras account still holding extras for this stockpile. */
  stockpileExtras?: Address<TAccountStockpileExtras>;
  /** Board used to enforce that this stockpile is still unresolved. */
  board?: Address<TAccountBoard>;
  /** Treasury PDA that owns the stockpile-extra custody accounts. */
  treasury?: Address<TAccountTreasury>;
  /** Classic SPL mint being paid to the resolved stockpile winner. */
  extraMint: Address<TAccountExtraMint>;
  stockpileExtraTokenAccount: Address<TAccountStockpileExtraTokenAccount>;
  winner: Address<TAccountWinner>;
  winnerExtraTokenAccount: Address<TAccountWinnerExtraTokenAccount>;
  /** Associated Token Program used to create the winner ATA on demand. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** Classic SPL Token Program used by v1 stockpile extras. */
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  extraIndex: PayoutStockpileExtraInstructionDataArgs["extraIndex"];
};

export async function getPayoutStockpileExtraInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountStockpile extends string,
  TAccountStockpileExtras extends string,
  TAccountBoard extends string,
  TAccountTreasury extends string,
  TAccountExtraMint extends string,
  TAccountStockpileExtraTokenAccount extends string,
  TAccountWinner extends string,
  TAccountWinnerExtraTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: PayoutStockpileExtraAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountStockpile,
    TAccountStockpileExtras,
    TAccountBoard,
    TAccountTreasury,
    TAccountExtraMint,
    TAccountStockpileExtraTokenAccount,
    TAccountWinner,
    TAccountWinnerExtraTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PayoutStockpileExtraInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountStockpile,
    TAccountStockpileExtras,
    TAccountBoard,
    TAccountTreasury,
    TAccountExtraMint,
    TAccountStockpileExtraTokenAccount,
    TAccountWinner,
    TAccountWinnerExtraTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    stockpile: { value: input.stockpile ?? null, isWritable: true },
    stockpileExtras: { value: input.stockpileExtras ?? null, isWritable: true },
    board: { value: input.board ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    extraMint: { value: input.extraMint ?? null, isWritable: false },
    stockpileExtraTokenAccount: {
      value: input.stockpileExtraTokenAccount ?? null,
      isWritable: true,
    },
    winner: { value: input.winner ?? null, isWritable: true },
    winnerExtraTokenAccount: {
      value: input.winnerExtraTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.stockpileExtras.value) {
    accounts.stockpileExtras.value = await findStockpileExtrasPda();
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("stockpile", accounts.stockpile),
      getAccountMeta("stockpileExtras", accounts.stockpileExtras),
      getAccountMeta("board", accounts.board),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("extraMint", accounts.extraMint),
      getAccountMeta(
        "stockpileExtraTokenAccount",
        accounts.stockpileExtraTokenAccount,
      ),
      getAccountMeta("winner", accounts.winner),
      getAccountMeta(
        "winnerExtraTokenAccount",
        accounts.winnerExtraTokenAccount,
      ),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPayoutStockpileExtraInstructionDataEncoder().encode(
      args as PayoutStockpileExtraInstructionDataArgs,
    ),
    programAddress,
  } as PayoutStockpileExtraInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountStockpile,
    TAccountStockpileExtras,
    TAccountBoard,
    TAccountTreasury,
    TAccountExtraMint,
    TAccountStockpileExtraTokenAccount,
    TAccountWinner,
    TAccountWinnerExtraTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type PayoutStockpileExtraInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountStockpile extends string = string,
  TAccountStockpileExtras extends string = string,
  TAccountBoard extends string = string,
  TAccountTreasury extends string = string,
  TAccountExtraMint extends string = string,
  TAccountStockpileExtraTokenAccount extends string = string,
  TAccountWinner extends string = string,
  TAccountWinnerExtraTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Crank signer that executes one extra-prize payout. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority. */
  config: Address<TAccountConfig>;
  /** Stockpile whose extra-prize payout is being processed. */
  stockpile: Address<TAccountStockpile>;
  /** Singleton pending-extras account still holding extras for this stockpile. */
  stockpileExtras: Address<TAccountStockpileExtras>;
  /** Board used to enforce that this stockpile is still unresolved. */
  board: Address<TAccountBoard>;
  /** Treasury PDA that owns the stockpile-extra custody accounts. */
  treasury: Address<TAccountTreasury>;
  /** Classic SPL mint being paid to the resolved stockpile winner. */
  extraMint: Address<TAccountExtraMint>;
  stockpileExtraTokenAccount: Address<TAccountStockpileExtraTokenAccount>;
  winner: Address<TAccountWinner>;
  winnerExtraTokenAccount: Address<TAccountWinnerExtraTokenAccount>;
  /** Associated Token Program used to create the winner ATA on demand. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** Classic SPL Token Program used by v1 stockpile extras. */
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  extraIndex: PayoutStockpileExtraInstructionDataArgs["extraIndex"];
};

export function getPayoutStockpileExtraInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountStockpile extends string,
  TAccountStockpileExtras extends string,
  TAccountBoard extends string,
  TAccountTreasury extends string,
  TAccountExtraMint extends string,
  TAccountStockpileExtraTokenAccount extends string,
  TAccountWinner extends string,
  TAccountWinnerExtraTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: PayoutStockpileExtraInput<
    TAccountSigner,
    TAccountConfig,
    TAccountStockpile,
    TAccountStockpileExtras,
    TAccountBoard,
    TAccountTreasury,
    TAccountExtraMint,
    TAccountStockpileExtraTokenAccount,
    TAccountWinner,
    TAccountWinnerExtraTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PayoutStockpileExtraInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountStockpile,
  TAccountStockpileExtras,
  TAccountBoard,
  TAccountTreasury,
  TAccountExtraMint,
  TAccountStockpileExtraTokenAccount,
  TAccountWinner,
  TAccountWinnerExtraTokenAccount,
  TAccountAssociatedTokenProgram,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    stockpile: { value: input.stockpile ?? null, isWritable: true },
    stockpileExtras: { value: input.stockpileExtras ?? null, isWritable: true },
    board: { value: input.board ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    extraMint: { value: input.extraMint ?? null, isWritable: false },
    stockpileExtraTokenAccount: {
      value: input.stockpileExtraTokenAccount ?? null,
      isWritable: true,
    },
    winner: { value: input.winner ?? null, isWritable: true },
    winnerExtraTokenAccount: {
      value: input.winnerExtraTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("stockpile", accounts.stockpile),
      getAccountMeta("stockpileExtras", accounts.stockpileExtras),
      getAccountMeta("board", accounts.board),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("extraMint", accounts.extraMint),
      getAccountMeta(
        "stockpileExtraTokenAccount",
        accounts.stockpileExtraTokenAccount,
      ),
      getAccountMeta("winner", accounts.winner),
      getAccountMeta(
        "winnerExtraTokenAccount",
        accounts.winnerExtraTokenAccount,
      ),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPayoutStockpileExtraInstructionDataEncoder().encode(
      args as PayoutStockpileExtraInstructionDataArgs,
    ),
    programAddress,
  } as PayoutStockpileExtraInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountStockpile,
    TAccountStockpileExtras,
    TAccountBoard,
    TAccountTreasury,
    TAccountExtraMint,
    TAccountStockpileExtraTokenAccount,
    TAccountWinner,
    TAccountWinnerExtraTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedPayoutStockpileExtraInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank signer that executes one extra-prize payout. */
    signer: TAccountMetas[0];
    /** Global config containing the crank authority. */
    config: TAccountMetas[1];
    /** Stockpile whose extra-prize payout is being processed. */
    stockpile: TAccountMetas[2];
    /** Singleton pending-extras account still holding extras for this stockpile. */
    stockpileExtras: TAccountMetas[3];
    /** Board used to enforce that this stockpile is still unresolved. */
    board: TAccountMetas[4];
    /** Treasury PDA that owns the stockpile-extra custody accounts. */
    treasury: TAccountMetas[5];
    /** Classic SPL mint being paid to the resolved stockpile winner. */
    extraMint: TAccountMetas[6];
    stockpileExtraTokenAccount: TAccountMetas[7];
    winner: TAccountMetas[8];
    winnerExtraTokenAccount: TAccountMetas[9];
    /** Associated Token Program used to create the winner ATA on demand. */
    associatedTokenProgram: TAccountMetas[10];
    /** Classic SPL Token Program used by v1 stockpile extras. */
    tokenProgram: TAccountMetas[11];
    systemProgram: TAccountMetas[12];
  };
  data: PayoutStockpileExtraInstructionData;
};

export function parsePayoutStockpileExtraInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPayoutStockpileExtraInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 13) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 13,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      stockpile: getNextAccount(),
      stockpileExtras: getNextAccount(),
      board: getNextAccount(),
      treasury: getNextAccount(),
      extraMint: getNextAccount(),
      stockpileExtraTokenAccount: getNextAccount(),
      winner: getNextAccount(),
      winnerExtraTokenAccount: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPayoutStockpileExtraInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
