/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBoardPda, findConfigPda, findTreasuryPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_PRIVATE_ZK_SINGLE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([185, 159, 15, 71, 10, 199, 38, 132]);

export function getSettlePrivateZkSingleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_PRIVATE_ZK_SINGLE_DISCRIMINATOR,
  );
}

export type SettlePrivateZkSingleInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountMiner extends string | AccountMeta<string> = string,
  TAccountPlayerProfile extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBoard extends string
        ? ReadonlyAccount<TAccountBoard>
        : TAccountBoard,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountMiner extends string
        ? WritableAccount<TAccountMiner>
        : TAccountMiner,
      TAccountPlayerProfile extends string
        ? WritableAccount<TAccountPlayerProfile>
        : TAccountPlayerProfile,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      ...TRemainingAccounts,
    ]
  >;

export type SettlePrivateZkSingleInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** Groth16 proof bytes in the program verifier wire format. */
  proof: ReadonlyUint8Array;
  /** Public proof output for the miner's effective stake on the winning tile. */
  winningStake: bigint;
  /** Public proof output for the count of selected tiles in the private mask. */
  selectedCount: number;
};

export type SettlePrivateZkSingleInstructionDataArgs = {
  /** Groth16 proof bytes in the program verifier wire format. */
  proof: ReadonlyUint8Array;
  /** Public proof output for the miner's effective stake on the winning tile. */
  winningStake: number | bigint;
  /** Public proof output for the count of selected tiles in the private mask. */
  selectedCount: number;
};

export function getSettlePrivateZkSingleInstructionDataEncoder(): FixedSizeEncoder<SettlePrivateZkSingleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["proof", fixEncoderSize(getBytesEncoder(), 256)],
      ["winningStake", getU64Encoder()],
      ["selectedCount", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SETTLE_PRIVATE_ZK_SINGLE_DISCRIMINATOR,
    }),
  );
}

export function getSettlePrivateZkSingleInstructionDataDecoder(): FixedSizeDecoder<SettlePrivateZkSingleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["proof", fixDecoderSize(getBytesDecoder(), 256)],
    ["winningStake", getU64Decoder()],
    ["selectedCount", getU8Decoder()],
  ]);
}

export function getSettlePrivateZkSingleInstructionDataCodec(): FixedSizeCodec<
  SettlePrivateZkSingleInstructionDataArgs,
  SettlePrivateZkSingleInstructionData
> {
  return combineCodec(
    getSettlePrivateZkSingleInstructionDataEncoder(),
    getSettlePrivateZkSingleInstructionDataDecoder(),
  );
}

export type SettlePrivateZkSingleAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountMiner extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountTreasury extends string = string,
> = {
  /** Crank signer authorized to submit settlement proofs. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing settlement capability and crank authority. */
  config?: Address<TAccountConfig>;
  /** Board authority source for crank checks. */
  board?: Address<TAccountBoard>;
  /** Revealed round currently waiting on per-miner settlement. */
  round: Address<TAccountRound>;
  /** Miner whose private mask is being settled. */
  miner: Address<TAccountMiner>;
  /** Player profile that receives the hidden-bonus bricks. */
  playerProfile: Address<TAccountPlayerProfile>;
  /** Treasury Bonanza state that may snapshot the live pot when this is the final miner. */
  treasury?: Address<TAccountTreasury>;
  proof: SettlePrivateZkSingleInstructionDataArgs["proof"];
  winningStake: SettlePrivateZkSingleInstructionDataArgs["winningStake"];
  selectedCount: SettlePrivateZkSingleInstructionDataArgs["selectedCount"];
};

export async function getSettlePrivateZkSingleInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountMiner extends string,
  TAccountPlayerProfile extends string,
  TAccountTreasury extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: SettlePrivateZkSingleAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountTreasury
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettlePrivateZkSingleInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountTreasury
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    miner: { value: input.miner ?? null, isWritable: true },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("miner", accounts.miner),
      getAccountMeta("playerProfile", accounts.playerProfile),
      getAccountMeta("treasury", accounts.treasury),
    ],
    data: getSettlePrivateZkSingleInstructionDataEncoder().encode(
      args as SettlePrivateZkSingleInstructionDataArgs,
    ),
    programAddress,
  } as SettlePrivateZkSingleInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountTreasury
  >);
}

export type SettlePrivateZkSingleInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountMiner extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountTreasury extends string = string,
> = {
  /** Crank signer authorized to submit settlement proofs. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing settlement capability and crank authority. */
  config: Address<TAccountConfig>;
  /** Board authority source for crank checks. */
  board: Address<TAccountBoard>;
  /** Revealed round currently waiting on per-miner settlement. */
  round: Address<TAccountRound>;
  /** Miner whose private mask is being settled. */
  miner: Address<TAccountMiner>;
  /** Player profile that receives the hidden-bonus bricks. */
  playerProfile: Address<TAccountPlayerProfile>;
  /** Treasury Bonanza state that may snapshot the live pot when this is the final miner. */
  treasury: Address<TAccountTreasury>;
  proof: SettlePrivateZkSingleInstructionDataArgs["proof"];
  winningStake: SettlePrivateZkSingleInstructionDataArgs["winningStake"];
  selectedCount: SettlePrivateZkSingleInstructionDataArgs["selectedCount"];
};

export function getSettlePrivateZkSingleInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountMiner extends string,
  TAccountPlayerProfile extends string,
  TAccountTreasury extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: SettlePrivateZkSingleInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountTreasury
  >,
  config?: { programAddress?: TProgramAddress },
): SettlePrivateZkSingleInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountBoard,
  TAccountRound,
  TAccountMiner,
  TAccountPlayerProfile,
  TAccountTreasury
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    miner: { value: input.miner ?? null, isWritable: true },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("miner", accounts.miner),
      getAccountMeta("playerProfile", accounts.playerProfile),
      getAccountMeta("treasury", accounts.treasury),
    ],
    data: getSettlePrivateZkSingleInstructionDataEncoder().encode(
      args as SettlePrivateZkSingleInstructionDataArgs,
    ),
    programAddress,
  } as SettlePrivateZkSingleInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountTreasury
  >);
}

export type ParsedSettlePrivateZkSingleInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank signer authorized to submit settlement proofs. */
    signer: TAccountMetas[0];
    /** Global config containing settlement capability and crank authority. */
    config: TAccountMetas[1];
    /** Board authority source for crank checks. */
    board: TAccountMetas[2];
    /** Revealed round currently waiting on per-miner settlement. */
    round: TAccountMetas[3];
    /** Miner whose private mask is being settled. */
    miner: TAccountMetas[4];
    /** Player profile that receives the hidden-bonus bricks. */
    playerProfile: TAccountMetas[5];
    /** Treasury Bonanza state that may snapshot the live pot when this is the final miner. */
    treasury: TAccountMetas[6];
  };
  data: SettlePrivateZkSingleInstructionData;
};

export function parseSettlePrivateZkSingleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettlePrivateZkSingleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      board: getNextAccount(),
      round: getNextAccount(),
      miner: getNextAccount(),
      playerProfile: getNextAccount(),
      treasury: getNextAccount(),
    },
    data: getSettlePrivateZkSingleInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
