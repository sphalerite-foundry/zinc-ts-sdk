/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAutoMinerSessionPda,
  findBoardPda,
  findBuybackSolVaultPda,
  findConfigPda,
  findDeployRoundFromAutoSessionPlayerProfilePda,
  findStockpileSolVaultPda,
  findTreasuryPda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const DEPLOY_ROUND_FROM_AUTO_SESSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([71, 122, 22, 191, 113, 52, 245, 81]);

export function getDeployRoundFromAutoSessionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPLOY_ROUND_FROM_AUTO_SESSION_DISCRIMINATOR,
  );
}

export type DeployRoundFromAutoSessionInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountExecutor extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAutoMinerSession extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMiner extends string | AccountMeta<string> = string,
  TAccountPlayerProfile extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountStockpileSolVault extends string | AccountMeta<string> = string,
  TAccountBuybackSolVault extends string | AccountMeta<string> = string,
  TAccountStockpile extends string | AccountMeta<string> = string,
  TAccountAffiliate extends string | AccountMeta<string> = string,
  TAccountAffiliateProfile extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountExecutor extends string
        ? WritableSignerAccount<TAccountExecutor> &
            AccountSignerMeta<TAccountExecutor>
        : TAccountExecutor,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountAutoMinerSession extends string
        ? WritableAccount<TAccountAutoMinerSession>
        : TAccountAutoMinerSession,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMiner extends string
        ? WritableAccount<TAccountMiner>
        : TAccountMiner,
      TAccountPlayerProfile extends string
        ? WritableAccount<TAccountPlayerProfile>
        : TAccountPlayerProfile,
      TAccountBoard extends string
        ? WritableAccount<TAccountBoard>
        : TAccountBoard,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountStockpileSolVault extends string
        ? WritableAccount<TAccountStockpileSolVault>
        : TAccountStockpileSolVault,
      TAccountBuybackSolVault extends string
        ? WritableAccount<TAccountBuybackSolVault>
        : TAccountBuybackSolVault,
      TAccountStockpile extends string
        ? ReadonlyAccount<TAccountStockpile>
        : TAccountStockpile,
      TAccountAffiliate extends string
        ? ReadonlyAccount<TAccountAffiliate>
        : TAccountAffiliate,
      TAccountAffiliateProfile extends string
        ? WritableAccount<TAccountAffiliateProfile>
        : TAccountAffiliateProfile,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DeployRoundFromAutoSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** X25519 public key used to encrypt the hidden tile mask for Arcium. */
  maskEncryptionKey: ReadonlyUint8Array;
  /** Rescue CTR nonce used for the encrypted tile mask payload. */
  maskNonce: bigint;
  /** Ciphertext limbs for the encrypted hidden tile mask payload. */
  maskCiphertext: ReadonlyUint8Array;
};

export type DeployRoundFromAutoSessionInstructionDataArgs = {
  /** X25519 public key used to encrypt the hidden tile mask for Arcium. */
  maskEncryptionKey: ReadonlyUint8Array;
  /** Rescue CTR nonce used for the encrypted tile mask payload. */
  maskNonce: number | bigint;
  /** Ciphertext limbs for the encrypted hidden tile mask payload. */
  maskCiphertext: ReadonlyUint8Array;
};

export function getDeployRoundFromAutoSessionInstructionDataEncoder(): FixedSizeEncoder<DeployRoundFromAutoSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["maskEncryptionKey", fixEncoderSize(getBytesEncoder(), 32)],
      ["maskNonce", getU128Encoder()],
      ["maskCiphertext", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({
      ...value,
      discriminator: DEPLOY_ROUND_FROM_AUTO_SESSION_DISCRIMINATOR,
    }),
  );
}

export function getDeployRoundFromAutoSessionInstructionDataDecoder(): FixedSizeDecoder<DeployRoundFromAutoSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["maskEncryptionKey", fixDecoderSize(getBytesDecoder(), 32)],
    ["maskNonce", getU128Decoder()],
    ["maskCiphertext", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

export function getDeployRoundFromAutoSessionInstructionDataCodec(): FixedSizeCodec<
  DeployRoundFromAutoSessionInstructionDataArgs,
  DeployRoundFromAutoSessionInstructionData
> {
  return combineCodec(
    getDeployRoundFromAutoSessionInstructionDataEncoder(),
    getDeployRoundFromAutoSessionInstructionDataDecoder(),
  );
}

export type DeployRoundFromAutoSessionAsyncInput<
  TAccountExecutor extends string = string,
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountRound extends string = string,
  TAccountConfig extends string = string,
  TAccountMiner extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountBoard extends string = string,
  TAccountTreasury extends string = string,
  TAccountStockpileSolVault extends string = string,
  TAccountBuybackSolVault extends string = string,
  TAccountStockpile extends string = string,
  TAccountAffiliate extends string = string,
  TAccountAffiliateProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Configured crank executing the auto deploy. */
  executor: TransactionSigner<TAccountExecutor>;
  authority: Address<TAccountAuthority>;
  /** Player-funded auto-miner session PDA. */
  autoMinerSession?: Address<TAccountAutoMinerSession>;
  /** Round receiving the auto deploy. */
  round: Address<TAccountRound>;
  /** Protocol config containing fee settings and crank authority. */
  config?: Address<TAccountConfig>;
  /** Miner position created for the session authority. */
  miner: Address<TAccountMiner>;
  /** Lifetime deploy profile for the auto-miner authority. */
  playerProfile?: Address<TAccountPlayerProfile>;
  /** Board singleton that tracks active and waiting rounds. */
  board?: Address<TAccountBoard>;
  /** Treasury PDA receiving protocol fees. */
  treasury?: Address<TAccountTreasury>;
  /** Program-owned lamport vault that accumulates stockpile SOL across cycles. */
  stockpileSolVault?: Address<TAccountStockpileSolVault>;
  /** Program-owned lamport vault that accumulates buyback SOL across deploys. */
  buybackSolVault?: Address<TAccountBuybackSolVault>;
  /** Active stockpile account, omitted when no cycle is currently joinable. */
  stockpile?: Address<TAccountStockpile>;
  affiliate?: Address<TAccountAffiliate>;
  affiliateProfile?: Address<TAccountAffiliateProfile>;
  /** System program used for PDA-funded transfers. */
  systemProgram?: Address<TAccountSystemProgram>;
  maskEncryptionKey: DeployRoundFromAutoSessionInstructionDataArgs["maskEncryptionKey"];
  maskNonce: DeployRoundFromAutoSessionInstructionDataArgs["maskNonce"];
  maskCiphertext: DeployRoundFromAutoSessionInstructionDataArgs["maskCiphertext"];
};

export async function getDeployRoundFromAutoSessionInstructionAsync<
  TAccountExecutor extends string,
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TAccountRound extends string,
  TAccountConfig extends string,
  TAccountMiner extends string,
  TAccountPlayerProfile extends string,
  TAccountBoard extends string,
  TAccountTreasury extends string,
  TAccountStockpileSolVault extends string,
  TAccountBuybackSolVault extends string,
  TAccountStockpile extends string,
  TAccountAffiliate extends string,
  TAccountAffiliateProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: DeployRoundFromAutoSessionAsyncInput<
    TAccountExecutor,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountRound,
    TAccountConfig,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountBoard,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountBuybackSolVault,
    TAccountStockpile,
    TAccountAffiliate,
    TAccountAffiliateProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeployRoundFromAutoSessionInstruction<
    TProgramAddress,
    TAccountExecutor,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountRound,
    TAccountConfig,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountBoard,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountBuybackSolVault,
    TAccountStockpile,
    TAccountAffiliate,
    TAccountAffiliateProfile,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    executor: { value: input.executor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    round: { value: input.round ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    miner: { value: input.miner ?? null, isWritable: true },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    board: { value: input.board ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    stockpileSolVault: {
      value: input.stockpileSolVault ?? null,
      isWritable: true,
    },
    buybackSolVault: { value: input.buybackSolVault ?? null, isWritable: true },
    stockpile: { value: input.stockpile ?? null, isWritable: false },
    affiliate: { value: input.affiliate ?? null, isWritable: false },
    affiliateProfile: {
      value: input.affiliateProfile ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.autoMinerSession.value) {
    accounts.autoMinerSession.value = await findAutoMinerSessionPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.playerProfile.value) {
    accounts.playerProfile.value =
      await findDeployRoundFromAutoSessionPlayerProfilePda({
        authority: getAddressFromResolvedInstructionAccount(
          "authority",
          accounts.authority.value,
        ),
      });
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.stockpileSolVault.value) {
    accounts.stockpileSolVault.value = await findStockpileSolVaultPda();
  }
  if (!accounts.buybackSolVault.value) {
    accounts.buybackSolVault.value = await findBuybackSolVaultPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("executor", accounts.executor),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("round", accounts.round),
      getAccountMeta("config", accounts.config),
      getAccountMeta("miner", accounts.miner),
      getAccountMeta("playerProfile", accounts.playerProfile),
      getAccountMeta("board", accounts.board),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("stockpileSolVault", accounts.stockpileSolVault),
      getAccountMeta("buybackSolVault", accounts.buybackSolVault),
      getAccountMeta("stockpile", accounts.stockpile),
      getAccountMeta("affiliate", accounts.affiliate),
      getAccountMeta("affiliateProfile", accounts.affiliateProfile),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeployRoundFromAutoSessionInstructionDataEncoder().encode(
      args as DeployRoundFromAutoSessionInstructionDataArgs,
    ),
    programAddress,
  } as DeployRoundFromAutoSessionInstruction<
    TProgramAddress,
    TAccountExecutor,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountRound,
    TAccountConfig,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountBoard,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountBuybackSolVault,
    TAccountStockpile,
    TAccountAffiliate,
    TAccountAffiliateProfile,
    TAccountSystemProgram
  >);
}

export type DeployRoundFromAutoSessionInput<
  TAccountExecutor extends string = string,
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountRound extends string = string,
  TAccountConfig extends string = string,
  TAccountMiner extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountBoard extends string = string,
  TAccountTreasury extends string = string,
  TAccountStockpileSolVault extends string = string,
  TAccountBuybackSolVault extends string = string,
  TAccountStockpile extends string = string,
  TAccountAffiliate extends string = string,
  TAccountAffiliateProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Configured crank executing the auto deploy. */
  executor: TransactionSigner<TAccountExecutor>;
  authority: Address<TAccountAuthority>;
  /** Player-funded auto-miner session PDA. */
  autoMinerSession: Address<TAccountAutoMinerSession>;
  /** Round receiving the auto deploy. */
  round: Address<TAccountRound>;
  /** Protocol config containing fee settings and crank authority. */
  config: Address<TAccountConfig>;
  /** Miner position created for the session authority. */
  miner: Address<TAccountMiner>;
  /** Lifetime deploy profile for the auto-miner authority. */
  playerProfile: Address<TAccountPlayerProfile>;
  /** Board singleton that tracks active and waiting rounds. */
  board: Address<TAccountBoard>;
  /** Treasury PDA receiving protocol fees. */
  treasury: Address<TAccountTreasury>;
  /** Program-owned lamport vault that accumulates stockpile SOL across cycles. */
  stockpileSolVault: Address<TAccountStockpileSolVault>;
  /** Program-owned lamport vault that accumulates buyback SOL across deploys. */
  buybackSolVault: Address<TAccountBuybackSolVault>;
  /** Active stockpile account, omitted when no cycle is currently joinable. */
  stockpile?: Address<TAccountStockpile>;
  affiliate?: Address<TAccountAffiliate>;
  affiliateProfile?: Address<TAccountAffiliateProfile>;
  /** System program used for PDA-funded transfers. */
  systemProgram?: Address<TAccountSystemProgram>;
  maskEncryptionKey: DeployRoundFromAutoSessionInstructionDataArgs["maskEncryptionKey"];
  maskNonce: DeployRoundFromAutoSessionInstructionDataArgs["maskNonce"];
  maskCiphertext: DeployRoundFromAutoSessionInstructionDataArgs["maskCiphertext"];
};

export function getDeployRoundFromAutoSessionInstruction<
  TAccountExecutor extends string,
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TAccountRound extends string,
  TAccountConfig extends string,
  TAccountMiner extends string,
  TAccountPlayerProfile extends string,
  TAccountBoard extends string,
  TAccountTreasury extends string,
  TAccountStockpileSolVault extends string,
  TAccountBuybackSolVault extends string,
  TAccountStockpile extends string,
  TAccountAffiliate extends string,
  TAccountAffiliateProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: DeployRoundFromAutoSessionInput<
    TAccountExecutor,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountRound,
    TAccountConfig,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountBoard,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountBuybackSolVault,
    TAccountStockpile,
    TAccountAffiliate,
    TAccountAffiliateProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DeployRoundFromAutoSessionInstruction<
  TProgramAddress,
  TAccountExecutor,
  TAccountAuthority,
  TAccountAutoMinerSession,
  TAccountRound,
  TAccountConfig,
  TAccountMiner,
  TAccountPlayerProfile,
  TAccountBoard,
  TAccountTreasury,
  TAccountStockpileSolVault,
  TAccountBuybackSolVault,
  TAccountStockpile,
  TAccountAffiliate,
  TAccountAffiliateProfile,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    executor: { value: input.executor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    round: { value: input.round ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    miner: { value: input.miner ?? null, isWritable: true },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    board: { value: input.board ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    stockpileSolVault: {
      value: input.stockpileSolVault ?? null,
      isWritable: true,
    },
    buybackSolVault: { value: input.buybackSolVault ?? null, isWritable: true },
    stockpile: { value: input.stockpile ?? null, isWritable: false },
    affiliate: { value: input.affiliate ?? null, isWritable: false },
    affiliateProfile: {
      value: input.affiliateProfile ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("executor", accounts.executor),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("round", accounts.round),
      getAccountMeta("config", accounts.config),
      getAccountMeta("miner", accounts.miner),
      getAccountMeta("playerProfile", accounts.playerProfile),
      getAccountMeta("board", accounts.board),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("stockpileSolVault", accounts.stockpileSolVault),
      getAccountMeta("buybackSolVault", accounts.buybackSolVault),
      getAccountMeta("stockpile", accounts.stockpile),
      getAccountMeta("affiliate", accounts.affiliate),
      getAccountMeta("affiliateProfile", accounts.affiliateProfile),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeployRoundFromAutoSessionInstructionDataEncoder().encode(
      args as DeployRoundFromAutoSessionInstructionDataArgs,
    ),
    programAddress,
  } as DeployRoundFromAutoSessionInstruction<
    TProgramAddress,
    TAccountExecutor,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountRound,
    TAccountConfig,
    TAccountMiner,
    TAccountPlayerProfile,
    TAccountBoard,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountBuybackSolVault,
    TAccountStockpile,
    TAccountAffiliate,
    TAccountAffiliateProfile,
    TAccountSystemProgram
  >);
}

export type ParsedDeployRoundFromAutoSessionInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Configured crank executing the auto deploy. */
    executor: TAccountMetas[0];
    authority: TAccountMetas[1];
    /** Player-funded auto-miner session PDA. */
    autoMinerSession: TAccountMetas[2];
    /** Round receiving the auto deploy. */
    round: TAccountMetas[3];
    /** Protocol config containing fee settings and crank authority. */
    config: TAccountMetas[4];
    /** Miner position created for the session authority. */
    miner: TAccountMetas[5];
    /** Lifetime deploy profile for the auto-miner authority. */
    playerProfile: TAccountMetas[6];
    /** Board singleton that tracks active and waiting rounds. */
    board: TAccountMetas[7];
    /** Treasury PDA receiving protocol fees. */
    treasury: TAccountMetas[8];
    /** Program-owned lamport vault that accumulates stockpile SOL across cycles. */
    stockpileSolVault: TAccountMetas[9];
    /** Program-owned lamport vault that accumulates buyback SOL across deploys. */
    buybackSolVault: TAccountMetas[10];
    /** Active stockpile account, omitted when no cycle is currently joinable. */
    stockpile?: TAccountMetas[11] | undefined;
    affiliate?: TAccountMetas[12] | undefined;
    affiliateProfile?: TAccountMetas[13] | undefined;
    /** System program used for PDA-funded transfers. */
    systemProgram: TAccountMetas[14];
  };
  data: DeployRoundFromAutoSessionInstructionData;
};

export function parseDeployRoundFromAutoSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeployRoundFromAutoSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 15) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 15,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === ZINC_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      executor: getNextAccount(),
      authority: getNextAccount(),
      autoMinerSession: getNextAccount(),
      round: getNextAccount(),
      config: getNextAccount(),
      miner: getNextAccount(),
      playerProfile: getNextAccount(),
      board: getNextAccount(),
      treasury: getNextAccount(),
      stockpileSolVault: getNextAccount(),
      buybackSolVault: getNextAccount(),
      stockpile: getNextOptionalAccount(),
      affiliate: getNextOptionalAccount(),
      affiliateProfile: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDeployRoundFromAutoSessionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
