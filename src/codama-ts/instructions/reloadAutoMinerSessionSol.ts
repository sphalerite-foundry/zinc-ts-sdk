/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAutoMinerSessionPda,
  findConfigPda,
  findDeployRoundFromAutoSessionPlayerProfilePda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const RELOAD_AUTO_MINER_SESSION_SOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([26, 170, 147, 196, 38, 148, 159, 234]);

export function getReloadAutoMinerSessionSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RELOAD_AUTO_MINER_SESSION_SOL_DISCRIMINATOR,
  );
}

export type ReloadAutoMinerSessionSolInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountExecutor extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAutoMinerSession extends string | AccountMeta<string> = string,
  TAccountPlayerProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountExecutor extends string
        ? WritableSignerAccount<TAccountExecutor> &
            AccountSignerMeta<TAccountExecutor>
        : TAccountExecutor,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountAutoMinerSession extends string
        ? WritableAccount<TAccountAutoMinerSession>
        : TAccountAutoMinerSession,
      TAccountPlayerProfile extends string
        ? WritableAccount<TAccountPlayerProfile>
        : TAccountPlayerProfile,
      ...TRemainingAccounts,
    ]
  >;

export type ReloadAutoMinerSessionSolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReloadAutoMinerSessionSolInstructionDataArgs = {};

export function getReloadAutoMinerSessionSolInstructionDataEncoder(): FixedSizeEncoder<ReloadAutoMinerSessionSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: RELOAD_AUTO_MINER_SESSION_SOL_DISCRIMINATOR,
    }),
  );
}

export function getReloadAutoMinerSessionSolInstructionDataDecoder(): FixedSizeDecoder<ReloadAutoMinerSessionSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReloadAutoMinerSessionSolInstructionDataCodec(): FixedSizeCodec<
  ReloadAutoMinerSessionSolInstructionDataArgs,
  ReloadAutoMinerSessionSolInstructionData
> {
  return combineCodec(
    getReloadAutoMinerSessionSolInstructionDataEncoder(),
    getReloadAutoMinerSessionSolInstructionDataDecoder(),
  );
}

export type ReloadAutoMinerSessionSolAsyncInput<
  TAccountExecutor extends string = string,
  TAccountConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountPlayerProfile extends string = string,
> = {
  /** Configured crank executing the reward reload. */
  executor: TransactionSigner<TAccountExecutor>;
  /** Protocol config containing the active crank authority. */
  config?: Address<TAccountConfig>;
  authority: Address<TAccountAuthority>;
  /** Auto-miner session PDA receiving the reloaded SOL budget. */
  autoMinerSession?: Address<TAccountAutoMinerSession>;
  /** Player profile PDA holding credited round SOL rewards. */
  playerProfile?: Address<TAccountPlayerProfile>;
};

export async function getReloadAutoMinerSessionSolInstructionAsync<
  TAccountExecutor extends string,
  TAccountConfig extends string,
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TAccountPlayerProfile extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: ReloadAutoMinerSessionSolAsyncInput<
    TAccountExecutor,
    TAccountConfig,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountPlayerProfile
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ReloadAutoMinerSessionSolInstruction<
    TProgramAddress,
    TAccountExecutor,
    TAccountConfig,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountPlayerProfile
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    executor: { value: input.executor ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.autoMinerSession.value) {
    accounts.autoMinerSession.value = await findAutoMinerSessionPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.playerProfile.value) {
    accounts.playerProfile.value =
      await findDeployRoundFromAutoSessionPlayerProfilePda({
        authority: getAddressFromResolvedInstructionAccount(
          "authority",
          accounts.authority.value,
        ),
      });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("executor", accounts.executor),
      getAccountMeta("config", accounts.config),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("playerProfile", accounts.playerProfile),
    ],
    data: getReloadAutoMinerSessionSolInstructionDataEncoder().encode({}),
    programAddress,
  } as ReloadAutoMinerSessionSolInstruction<
    TProgramAddress,
    TAccountExecutor,
    TAccountConfig,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountPlayerProfile
  >);
}

export type ReloadAutoMinerSessionSolInput<
  TAccountExecutor extends string = string,
  TAccountConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountPlayerProfile extends string = string,
> = {
  /** Configured crank executing the reward reload. */
  executor: TransactionSigner<TAccountExecutor>;
  /** Protocol config containing the active crank authority. */
  config: Address<TAccountConfig>;
  authority: Address<TAccountAuthority>;
  /** Auto-miner session PDA receiving the reloaded SOL budget. */
  autoMinerSession: Address<TAccountAutoMinerSession>;
  /** Player profile PDA holding credited round SOL rewards. */
  playerProfile: Address<TAccountPlayerProfile>;
};

export function getReloadAutoMinerSessionSolInstruction<
  TAccountExecutor extends string,
  TAccountConfig extends string,
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TAccountPlayerProfile extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: ReloadAutoMinerSessionSolInput<
    TAccountExecutor,
    TAccountConfig,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountPlayerProfile
  >,
  config?: { programAddress?: TProgramAddress },
): ReloadAutoMinerSessionSolInstruction<
  TProgramAddress,
  TAccountExecutor,
  TAccountConfig,
  TAccountAuthority,
  TAccountAutoMinerSession,
  TAccountPlayerProfile
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    executor: { value: input.executor ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("executor", accounts.executor),
      getAccountMeta("config", accounts.config),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("playerProfile", accounts.playerProfile),
    ],
    data: getReloadAutoMinerSessionSolInstructionDataEncoder().encode({}),
    programAddress,
  } as ReloadAutoMinerSessionSolInstruction<
    TProgramAddress,
    TAccountExecutor,
    TAccountConfig,
    TAccountAuthority,
    TAccountAutoMinerSession,
    TAccountPlayerProfile
  >);
}

export type ParsedReloadAutoMinerSessionSolInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Configured crank executing the reward reload. */
    executor: TAccountMetas[0];
    /** Protocol config containing the active crank authority. */
    config: TAccountMetas[1];
    authority: TAccountMetas[2];
    /** Auto-miner session PDA receiving the reloaded SOL budget. */
    autoMinerSession: TAccountMetas[3];
    /** Player profile PDA holding credited round SOL rewards. */
    playerProfile: TAccountMetas[4];
  };
  data: ReloadAutoMinerSessionSolInstructionData;
};

export function parseReloadAutoMinerSessionSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReloadAutoMinerSessionSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      executor: getNextAccount(),
      config: getNextAccount(),
      authority: getNextAccount(),
      autoMinerSession: getNextAccount(),
      playerProfile: getNextAccount(),
    },
    data: getReloadAutoMinerSessionSolInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
