/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findBuybackSolVaultPda,
  findConfigPda,
  findTreasuryPda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const WRAP_BUYBACK_SOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([230, 81, 102, 81, 194, 180, 24, 22]);

export function getWrapBuybackSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WRAP_BUYBACK_SOL_DISCRIMINATOR
  );
}

export type WrapBuybackSolInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountBuybackSolVault extends string | AccountMeta<string> = string,
  TAccountWsolMint extends
    | string
    | AccountMeta<string> = "So11111111111111111111111111111111111111112",
  TAccountTreasuryWsolTokenAccount extends
    | string
    | AccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends
    | string
    | AccountMeta<string> = "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountTokenProgram extends
    | string
    | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountBuybackSolVault extends string
        ? WritableAccount<TAccountBuybackSolVault>
        : TAccountBuybackSolVault,
      TAccountWsolMint extends string
        ? ReadonlyAccount<TAccountWsolMint>
        : TAccountWsolMint,
      TAccountTreasuryWsolTokenAccount extends string
        ? WritableAccount<TAccountTreasuryWsolTokenAccount>
        : TAccountTreasuryWsolTokenAccount,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts
    ]
  >;

export type WrapBuybackSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** Exact lamports to move from the buyback SOL vault into treasury-owned WSOL. */
  amount: bigint;
};

export type WrapBuybackSolInstructionDataArgs = {
  /** Exact lamports to move from the buyback SOL vault into treasury-owned WSOL. */
  amount: number | bigint;
};

export function getWrapBuybackSolInstructionDataEncoder(): FixedSizeEncoder<WrapBuybackSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WRAP_BUYBACK_SOL_DISCRIMINATOR })
  );
}

export function getWrapBuybackSolInstructionDataDecoder(): FixedSizeDecoder<WrapBuybackSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getWrapBuybackSolInstructionDataCodec(): FixedSizeCodec<
  WrapBuybackSolInstructionDataArgs,
  WrapBuybackSolInstructionData
> {
  return combineCodec(
    getWrapBuybackSolInstructionDataEncoder(),
    getWrapBuybackSolInstructionDataDecoder()
  );
}

export type WrapBuybackSolAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountBuybackSolVault extends string = string,
  TAccountWsolMint extends string = string,
  TAccountTreasuryWsolTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string
> = {
  /** Crank authority that pays ATA rent when needed and requests the wrap. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority and treasury address. */
  config?: Address<TAccountConfig>;
  /** Treasury PDA that owns protocol token custody accounts. */
  treasury?: Address<TAccountTreasury>;
  /** Program-owned lamport vault that accumulates deploy-time buyback SOL. */
  buybackSolVault?: Address<TAccountBuybackSolVault>;
  /** SPL native mint used for treasury-owned wrapped SOL. */
  wsolMint?: Address<TAccountWsolMint>;
  /** Treasury-owned WSOL account that receives wrapped buyback SOL. */
  treasuryWsolTokenAccount?: Address<TAccountTreasuryWsolTokenAccount>;
  /** Associated Token Program used to create the treasury WSOL ATA. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the WSOL account. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used for ATA creation when the WSOL account is missing. */
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WrapBuybackSolInstructionDataArgs["amount"];
};

export async function getWrapBuybackSolInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountBuybackSolVault extends string,
  TAccountWsolMint extends string,
  TAccountTreasuryWsolTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: WrapBuybackSolAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountBuybackSolVault,
    TAccountWsolMint,
    TAccountTreasuryWsolTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  WrapBuybackSolInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountBuybackSolVault,
    TAccountWsolMint,
    TAccountTreasuryWsolTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    buybackSolVault: { value: input.buybackSolVault ?? null, isWritable: true },
    wsolMint: { value: input.wsolMint ?? null, isWritable: false },
    treasuryWsolTokenAccount: {
      value: input.treasuryWsolTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.buybackSolVault.value) {
    accounts.buybackSolVault.value = await findBuybackSolVaultPda();
  }
  if (!accounts.wsolMint.value) {
    accounts.wsolMint.value =
      "So11111111111111111111111111111111111111112" as Address<"So11111111111111111111111111111111111111112">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.treasuryWsolTokenAccount.value) {
    accounts.treasuryWsolTokenAccount.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "treasury",
            accounts.treasury.value
          )
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "tokenProgram",
            accounts.tokenProgram.value
          )
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "wsolMint",
            accounts.wsolMint.value
          )
        ),
      ],
    });
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("buybackSolVault", accounts.buybackSolVault),
      getAccountMeta("wsolMint", accounts.wsolMint),
      getAccountMeta(
        "treasuryWsolTokenAccount",
        accounts.treasuryWsolTokenAccount
      ),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getWrapBuybackSolInstructionDataEncoder().encode(
      args as WrapBuybackSolInstructionDataArgs
    ),
    programAddress,
  } as WrapBuybackSolInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountTreasury, TAccountBuybackSolVault, TAccountWsolMint, TAccountTreasuryWsolTokenAccount, TAccountAssociatedTokenProgram, TAccountTokenProgram, TAccountSystemProgram>);
}

export type WrapBuybackSolInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountBuybackSolVault extends string = string,
  TAccountWsolMint extends string = string,
  TAccountTreasuryWsolTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string
> = {
  /** Crank authority that pays ATA rent when needed and requests the wrap. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority and treasury address. */
  config: Address<TAccountConfig>;
  /** Treasury PDA that owns protocol token custody accounts. */
  treasury: Address<TAccountTreasury>;
  /** Program-owned lamport vault that accumulates deploy-time buyback SOL. */
  buybackSolVault: Address<TAccountBuybackSolVault>;
  /** SPL native mint used for treasury-owned wrapped SOL. */
  wsolMint?: Address<TAccountWsolMint>;
  /** Treasury-owned WSOL account that receives wrapped buyback SOL. */
  treasuryWsolTokenAccount: Address<TAccountTreasuryWsolTokenAccount>;
  /** Associated Token Program used to create the treasury WSOL ATA. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the WSOL account. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used for ATA creation when the WSOL account is missing. */
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WrapBuybackSolInstructionDataArgs["amount"];
};

export function getWrapBuybackSolInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountBuybackSolVault extends string,
  TAccountWsolMint extends string,
  TAccountTreasuryWsolTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: WrapBuybackSolInput<
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountBuybackSolVault,
    TAccountWsolMint,
    TAccountTreasuryWsolTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): WrapBuybackSolInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountTreasury,
  TAccountBuybackSolVault,
  TAccountWsolMint,
  TAccountTreasuryWsolTokenAccount,
  TAccountAssociatedTokenProgram,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    buybackSolVault: { value: input.buybackSolVault ?? null, isWritable: true },
    wsolMint: { value: input.wsolMint ?? null, isWritable: false },
    treasuryWsolTokenAccount: {
      value: input.treasuryWsolTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.wsolMint.value) {
    accounts.wsolMint.value =
      "So11111111111111111111111111111111111111112" as Address<"So11111111111111111111111111111111111111112">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("buybackSolVault", accounts.buybackSolVault),
      getAccountMeta("wsolMint", accounts.wsolMint),
      getAccountMeta(
        "treasuryWsolTokenAccount",
        accounts.treasuryWsolTokenAccount
      ),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getWrapBuybackSolInstructionDataEncoder().encode(
      args as WrapBuybackSolInstructionDataArgs
    ),
    programAddress,
  } as WrapBuybackSolInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountTreasury, TAccountBuybackSolVault, TAccountWsolMint, TAccountTreasuryWsolTokenAccount, TAccountAssociatedTokenProgram, TAccountTokenProgram, TAccountSystemProgram>);
}

export type ParsedWrapBuybackSolInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank authority that pays ATA rent when needed and requests the wrap. */
    signer: TAccountMetas[0];
    /** Global config containing the crank authority and treasury address. */
    config: TAccountMetas[1];
    /** Treasury PDA that owns protocol token custody accounts. */
    treasury: TAccountMetas[2];
    /** Program-owned lamport vault that accumulates deploy-time buyback SOL. */
    buybackSolVault: TAccountMetas[3];
    /** SPL native mint used for treasury-owned wrapped SOL. */
    wsolMint: TAccountMetas[4];
    /** Treasury-owned WSOL account that receives wrapped buyback SOL. */
    treasuryWsolTokenAccount: TAccountMetas[5];
    /** Associated Token Program used to create the treasury WSOL ATA. */
    associatedTokenProgram: TAccountMetas[6];
    /** SPL Token Program that owns the WSOL account. */
    tokenProgram: TAccountMetas[7];
    /** System Program used for ATA creation when the WSOL account is missing. */
    systemProgram: TAccountMetas[8];
  };
  data: WrapBuybackSolInstructionData;
};

export function parseWrapBuybackSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[]
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWrapBuybackSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      treasury: getNextAccount(),
      buybackSolVault: getNextAccount(),
      wsolMint: getNextAccount(),
      treasuryWsolTokenAccount: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWrapBuybackSolInstructionDataDecoder().decode(instruction.data),
  };
}
