/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findBoardPda,
  findConfigPda,
  findStockpileSolVaultPda,
  findTreasuryPda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_STOCKPILE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [101, 6, 219, 243, 219, 170, 245, 147],
);

export function getCloseStockpileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_STOCKPILE_DISCRIMINATOR,
  );
}

export type CloseStockpileInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountStockpile extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountStockpileSolVault extends string | AccountMeta<string> = string,
  TAccountStockpileTokenAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBoard extends string
        ? WritableAccount<TAccountBoard>
        : TAccountBoard,
      TAccountStockpile extends string
        ? WritableAccount<TAccountStockpile>
        : TAccountStockpile,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountStockpileSolVault extends string
        ? WritableAccount<TAccountStockpileSolVault>
        : TAccountStockpileSolVault,
      TAccountStockpileTokenAccount extends string
        ? WritableAccount<TAccountStockpileTokenAccount>
        : TAccountStockpileTokenAccount,
      ...TRemainingAccounts,
    ]
  >;

export type CloseStockpileInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseStockpileInstructionDataArgs = {};

export function getCloseStockpileInstructionDataEncoder(): FixedSizeEncoder<CloseStockpileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_STOCKPILE_DISCRIMINATOR }),
  );
}

export function getCloseStockpileInstructionDataDecoder(): FixedSizeDecoder<CloseStockpileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseStockpileInstructionDataCodec(): FixedSizeCodec<
  CloseStockpileInstructionDataArgs,
  CloseStockpileInstructionData
> {
  return combineCodec(
    getCloseStockpileInstructionDataEncoder(),
    getCloseStockpileInstructionDataDecoder(),
  );
}

export type CloseStockpileAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountStockpile extends string = string,
  TAccountTreasury extends string = string,
  TAccountStockpileSolVault extends string = string,
  TAccountStockpileTokenAccount extends string = string,
> = {
  /** Crank authorized to close the active stockpile. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority. */
  config?: Address<TAccountConfig>;
  /** Board used to identify the currently open stockpile, if any. */
  board?: Address<TAccountBoard>;
  /** Open stockpile cycle being closed for reveal or carry-forward. */
  stockpile: Address<TAccountStockpile>;
  /** Treasury account that owns the stockpile reward vaults. */
  treasury?: Address<TAccountTreasury>;
  /** Program-owned lamport vault that funds stockpile SOL payouts. */
  stockpileSolVault?: Address<TAccountStockpileSolVault>;
  /** Dedicated stockpile token vault that funds stockpile ZINC payouts. */
  stockpileTokenAccount: Address<TAccountStockpileTokenAccount>;
};

export async function getCloseStockpileInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountStockpile extends string,
  TAccountTreasury extends string,
  TAccountStockpileSolVault extends string,
  TAccountStockpileTokenAccount extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseStockpileAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountStockpile,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountStockpileTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseStockpileInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountStockpile,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountStockpileTokenAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    stockpile: { value: input.stockpile ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    stockpileSolVault: {
      value: input.stockpileSolVault ?? null,
      isWritable: true,
    },
    stockpileTokenAccount: {
      value: input.stockpileTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.stockpileSolVault.value) {
    accounts.stockpileSolVault.value = await findStockpileSolVaultPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("stockpile", accounts.stockpile),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("stockpileSolVault", accounts.stockpileSolVault),
      getAccountMeta("stockpileTokenAccount", accounts.stockpileTokenAccount),
    ],
    data: getCloseStockpileInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStockpileInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountStockpile,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountStockpileTokenAccount
  >);
}

export type CloseStockpileInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountStockpile extends string = string,
  TAccountTreasury extends string = string,
  TAccountStockpileSolVault extends string = string,
  TAccountStockpileTokenAccount extends string = string,
> = {
  /** Crank authorized to close the active stockpile. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority. */
  config: Address<TAccountConfig>;
  /** Board used to identify the currently open stockpile, if any. */
  board: Address<TAccountBoard>;
  /** Open stockpile cycle being closed for reveal or carry-forward. */
  stockpile: Address<TAccountStockpile>;
  /** Treasury account that owns the stockpile reward vaults. */
  treasury: Address<TAccountTreasury>;
  /** Program-owned lamport vault that funds stockpile SOL payouts. */
  stockpileSolVault: Address<TAccountStockpileSolVault>;
  /** Dedicated stockpile token vault that funds stockpile ZINC payouts. */
  stockpileTokenAccount: Address<TAccountStockpileTokenAccount>;
};

export function getCloseStockpileInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountStockpile extends string,
  TAccountTreasury extends string,
  TAccountStockpileSolVault extends string,
  TAccountStockpileTokenAccount extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseStockpileInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountStockpile,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountStockpileTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): CloseStockpileInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountBoard,
  TAccountStockpile,
  TAccountTreasury,
  TAccountStockpileSolVault,
  TAccountStockpileTokenAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    stockpile: { value: input.stockpile ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    stockpileSolVault: {
      value: input.stockpileSolVault ?? null,
      isWritable: true,
    },
    stockpileTokenAccount: {
      value: input.stockpileTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("stockpile", accounts.stockpile),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("stockpileSolVault", accounts.stockpileSolVault),
      getAccountMeta("stockpileTokenAccount", accounts.stockpileTokenAccount),
    ],
    data: getCloseStockpileInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStockpileInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountStockpile,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountStockpileTokenAccount
  >);
}

export type ParsedCloseStockpileInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank authorized to close the active stockpile. */
    signer: TAccountMetas[0];
    /** Global config containing the crank authority. */
    config: TAccountMetas[1];
    /** Board used to identify the currently open stockpile, if any. */
    board: TAccountMetas[2];
    /** Open stockpile cycle being closed for reveal or carry-forward. */
    stockpile: TAccountMetas[3];
    /** Treasury account that owns the stockpile reward vaults. */
    treasury: TAccountMetas[4];
    /** Program-owned lamport vault that funds stockpile SOL payouts. */
    stockpileSolVault: TAccountMetas[5];
    /** Dedicated stockpile token vault that funds stockpile ZINC payouts. */
    stockpileTokenAccount: TAccountMetas[6];
  };
  data: CloseStockpileInstructionData;
};

export function parseCloseStockpileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseStockpileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      board: getNextAccount(),
      stockpile: getNextAccount(),
      treasury: getNextAccount(),
      stockpileSolVault: getNextAccount(),
      stockpileTokenAccount: getNextAccount(),
    },
    data: getCloseStockpileInstructionDataDecoder().decode(instruction.data),
  };
}
