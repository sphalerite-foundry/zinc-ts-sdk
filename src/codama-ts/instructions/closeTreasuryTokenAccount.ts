/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findTreasuryPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_TREASURY_TOKEN_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([39, 253, 164, 255, 181, 54, 22, 27]);

export function getCloseTreasuryTokenAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_TREASURY_TOKEN_ACCOUNT_DISCRIMINATOR,
  );
}

export type CloseTreasuryTokenAccountInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountSourceTokenAccount extends string | AccountMeta<string> = string,
  TAccountAdminTokenAccount extends string | AccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountSourceTokenAccount extends string
        ? WritableAccount<TAccountSourceTokenAccount>
        : TAccountSourceTokenAccount,
      TAccountAdminTokenAccount extends string
        ? WritableAccount<TAccountAdminTokenAccount>
        : TAccountAdminTokenAccount,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseTreasuryTokenAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseTreasuryTokenAccountInstructionDataArgs = {};

export function getCloseTreasuryTokenAccountInstructionDataEncoder(): FixedSizeEncoder<CloseTreasuryTokenAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_TREASURY_TOKEN_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

export function getCloseTreasuryTokenAccountInstructionDataDecoder(): FixedSizeDecoder<CloseTreasuryTokenAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseTreasuryTokenAccountInstructionDataCodec(): FixedSizeCodec<
  CloseTreasuryTokenAccountInstructionDataArgs,
  CloseTreasuryTokenAccountInstructionData
> {
  return combineCodec(
    getCloseTreasuryTokenAccountInstructionDataEncoder(),
    getCloseTreasuryTokenAccountInstructionDataDecoder(),
  );
}

export type CloseTreasuryTokenAccountAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountMint extends string = string,
  TAccountSourceTokenAccount extends string = string,
  TAccountAdminTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Admin signer that receives drained tokens and closed-account rent. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Global config that authorizes the admin and treasury relationship. */
  config?: Address<TAccountConfig>;
  /** Treasury PDA that owns the source token account being closed. */
  treasury?: Address<TAccountTreasury>;
  /** Mint for the token account being drained. */
  mint: Address<TAccountMint>;
  /** Treasury-owned SPL token account to drain and close. */
  sourceTokenAccount: Address<TAccountSourceTokenAccount>;
  /** Admin ATA that receives the drained token balance. */
  adminTokenAccount?: Address<TAccountAdminTokenAccount>;
  /** Associated Token Program used to create the admin ATA when absent. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the source and destination token accounts. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used by ATA creation. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCloseTreasuryTokenAccountInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountMint extends string,
  TAccountSourceTokenAccount extends string,
  TAccountAdminTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseTreasuryTokenAccountAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseTreasuryTokenAccountInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    sourceTokenAccount: {
      value: input.sourceTokenAccount ?? null,
      isWritable: true,
    },
    adminTokenAccount: {
      value: input.adminTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.adminTokenAccount.value) {
    accounts.adminTokenAccount.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "admin",
            accounts.admin.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "tokenProgram",
            accounts.tokenProgram.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("mint", accounts.mint.value),
        ),
      ],
    });
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("sourceTokenAccount", accounts.sourceTokenAccount),
      getAccountMeta("adminTokenAccount", accounts.adminTokenAccount),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseTreasuryTokenAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseTreasuryTokenAccountInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type CloseTreasuryTokenAccountInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountMint extends string = string,
  TAccountSourceTokenAccount extends string = string,
  TAccountAdminTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Admin signer that receives drained tokens and closed-account rent. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Global config that authorizes the admin and treasury relationship. */
  config: Address<TAccountConfig>;
  /** Treasury PDA that owns the source token account being closed. */
  treasury: Address<TAccountTreasury>;
  /** Mint for the token account being drained. */
  mint: Address<TAccountMint>;
  /** Treasury-owned SPL token account to drain and close. */
  sourceTokenAccount: Address<TAccountSourceTokenAccount>;
  /** Admin ATA that receives the drained token balance. */
  adminTokenAccount: Address<TAccountAdminTokenAccount>;
  /** Associated Token Program used to create the admin ATA when absent. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the source and destination token accounts. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used by ATA creation. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCloseTreasuryTokenAccountInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountMint extends string,
  TAccountSourceTokenAccount extends string,
  TAccountAdminTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseTreasuryTokenAccountInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseTreasuryTokenAccountInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountTreasury,
  TAccountMint,
  TAccountSourceTokenAccount,
  TAccountAdminTokenAccount,
  TAccountAssociatedTokenProgram,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    sourceTokenAccount: {
      value: input.sourceTokenAccount ?? null,
      isWritable: true,
    },
    adminTokenAccount: {
      value: input.adminTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("sourceTokenAccount", accounts.sourceTokenAccount),
      getAccountMeta("adminTokenAccount", accounts.adminTokenAccount),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseTreasuryTokenAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseTreasuryTokenAccountInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCloseTreasuryTokenAccountInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin signer that receives drained tokens and closed-account rent. */
    admin: TAccountMetas[0];
    /** Global config that authorizes the admin and treasury relationship. */
    config: TAccountMetas[1];
    /** Treasury PDA that owns the source token account being closed. */
    treasury: TAccountMetas[2];
    /** Mint for the token account being drained. */
    mint: TAccountMetas[3];
    /** Treasury-owned SPL token account to drain and close. */
    sourceTokenAccount: TAccountMetas[4];
    /** Admin ATA that receives the drained token balance. */
    adminTokenAccount: TAccountMetas[5];
    /** Associated Token Program used to create the admin ATA when absent. */
    associatedTokenProgram: TAccountMetas[6];
    /** SPL Token Program that owns the source and destination token accounts. */
    tokenProgram: TAccountMetas[7];
    /** System Program used by ATA creation. */
    systemProgram: TAccountMetas[8];
  };
  data: CloseTreasuryTokenAccountInstructionData;
};

export function parseCloseTreasuryTokenAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseTreasuryTokenAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      treasury: getNextAccount(),
      mint: getNextAccount(),
      sourceTokenAccount: getNextAccount(),
      adminTokenAccount: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseTreasuryTokenAccountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
