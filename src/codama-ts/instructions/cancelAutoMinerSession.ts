/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAutoMinerSessionPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_AUTO_MINER_SESSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([176, 79, 21, 217, 9, 144, 222, 12]);

export function getCancelAutoMinerSessionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_AUTO_MINER_SESSION_DISCRIMINATOR,
  );
}

export type CancelAutoMinerSessionInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAutoMinerSession extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAutoMinerSession extends string
        ? WritableAccount<TAccountAutoMinerSession>
        : TAccountAutoMinerSession,
      ...TRemainingAccounts,
    ]
  >;

export type CancelAutoMinerSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelAutoMinerSessionInstructionDataArgs = {};

export function getCancelAutoMinerSessionInstructionDataEncoder(): FixedSizeEncoder<CancelAutoMinerSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_AUTO_MINER_SESSION_DISCRIMINATOR,
    }),
  );
}

export function getCancelAutoMinerSessionInstructionDataDecoder(): FixedSizeDecoder<CancelAutoMinerSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelAutoMinerSessionInstructionDataCodec(): FixedSizeCodec<
  CancelAutoMinerSessionInstructionDataArgs,
  CancelAutoMinerSessionInstructionData
> {
  return combineCodec(
    getCancelAutoMinerSessionInstructionDataEncoder(),
    getCancelAutoMinerSessionInstructionDataDecoder(),
  );
}

export type CancelAutoMinerSessionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
> = {
  /** Wallet that owns and cancels the session. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Player-owned auto-miner session PDA being closed. */
  autoMinerSession?: Address<TAccountAutoMinerSession>;
};

export async function getCancelAutoMinerSessionInstructionAsync<
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CancelAutoMinerSessionAsyncInput<
    TAccountAuthority,
    TAccountAutoMinerSession
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelAutoMinerSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAutoMinerSession
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.autoMinerSession.value) {
    accounts.autoMinerSession.value = await findAutoMinerSessionPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
    ],
    data: getCancelAutoMinerSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelAutoMinerSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAutoMinerSession
  >);
}

export type CancelAutoMinerSessionInput<
  TAccountAuthority extends string = string,
  TAccountAutoMinerSession extends string = string,
> = {
  /** Wallet that owns and cancels the session. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Player-owned auto-miner session PDA being closed. */
  autoMinerSession: Address<TAccountAutoMinerSession>;
};

export function getCancelAutoMinerSessionInstruction<
  TAccountAuthority extends string,
  TAccountAutoMinerSession extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CancelAutoMinerSessionInput<
    TAccountAuthority,
    TAccountAutoMinerSession
  >,
  config?: { programAddress?: TProgramAddress },
): CancelAutoMinerSessionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAutoMinerSession
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
    ],
    data: getCancelAutoMinerSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelAutoMinerSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAutoMinerSession
  >);
}

export type ParsedCancelAutoMinerSessionInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet that owns and cancels the session. */
    authority: TAccountMetas[0];
    /** Player-owned auto-miner session PDA being closed. */
    autoMinerSession: TAccountMetas[1];
  };
  data: CancelAutoMinerSessionInstructionData;
};

export function parseCancelAutoMinerSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelAutoMinerSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      autoMinerSession: getNextAccount(),
    },
    data: getCancelAutoMinerSessionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
