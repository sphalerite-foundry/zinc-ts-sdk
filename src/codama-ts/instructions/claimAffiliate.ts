/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPlayerProfilePda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLAIM_AFFILIATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [12, 79, 157, 146, 92, 197, 95, 18]
);

export function getClaimAffiliateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_AFFILIATE_DISCRIMINATOR
  );
}

export type ClaimAffiliateInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPlayerProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPlayerProfile extends string
        ? WritableAccount<TAccountPlayerProfile>
        : TAccountPlayerProfile,
      ...TRemainingAccounts
    ]
  >;

export type ClaimAffiliateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClaimAffiliateInstructionDataArgs = {};

export function getClaimAffiliateInstructionDataEncoder(): FixedSizeEncoder<ClaimAffiliateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_AFFILIATE_DISCRIMINATOR })
  );
}

export function getClaimAffiliateInstructionDataDecoder(): FixedSizeDecoder<ClaimAffiliateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimAffiliateInstructionDataCodec(): FixedSizeCodec<
  ClaimAffiliateInstructionDataArgs,
  ClaimAffiliateInstructionData
> {
  return combineCodec(
    getClaimAffiliateInstructionDataEncoder(),
    getClaimAffiliateInstructionDataDecoder()
  );
}

export type ClaimAffiliateAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountPlayerProfile extends string = string
> = {
  /** Affiliate wallet claiming its accrued SOL balance. */
  signer: TransactionSigner<TAccountSigner>;
  /** Protocol config that can pause affiliate withdrawals. */
  config?: Address<TAccountConfig>;
  /** Player profile that now also stores pending and lifetime affiliate rewards. */
  playerProfile?: Address<TAccountPlayerProfile>;
};

export async function getClaimAffiliateInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountPlayerProfile extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: ClaimAffiliateAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountPlayerProfile
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClaimAffiliateInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountPlayerProfile
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.playerProfile.value) {
    accounts.playerProfile.value = await findPlayerProfilePda({
      signer: getAddressFromResolvedInstructionAccount(
        "signer",
        accounts.signer.value
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("playerProfile", accounts.playerProfile),
    ],
    data: getClaimAffiliateInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimAffiliateInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountPlayerProfile>);
}

export type ClaimAffiliateInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountPlayerProfile extends string = string
> = {
  /** Affiliate wallet claiming its accrued SOL balance. */
  signer: TransactionSigner<TAccountSigner>;
  /** Protocol config that can pause affiliate withdrawals. */
  config: Address<TAccountConfig>;
  /** Player profile that now also stores pending and lifetime affiliate rewards. */
  playerProfile: Address<TAccountPlayerProfile>;
};

export function getClaimAffiliateInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountPlayerProfile extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: ClaimAffiliateInput<
    TAccountSigner,
    TAccountConfig,
    TAccountPlayerProfile
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimAffiliateInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountPlayerProfile
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("playerProfile", accounts.playerProfile),
    ],
    data: getClaimAffiliateInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimAffiliateInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountPlayerProfile>);
}

export type ParsedClaimAffiliateInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Affiliate wallet claiming its accrued SOL balance. */
    signer: TAccountMetas[0];
    /** Protocol config that can pause affiliate withdrawals. */
    config: TAccountMetas[1];
    /** Player profile that now also stores pending and lifetime affiliate rewards. */
    playerProfile: TAccountMetas[2];
  };
  data: ClaimAffiliateInstructionData;
};

export function parseClaimAffiliateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[]
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimAffiliateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      playerProfile: getNextAccount(),
    },
    data: getClaimAffiliateInstructionDataDecoder().decode(instruction.data),
  };
}
