/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findConfigPda,
  findPlayerProfilePda,
  findRoundZincRewardTokenAccountPda,
  findTreasuryPda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLAIM_PLAYER_ZINC_REWARDS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([65, 17, 118, 123, 60, 247, 49, 165]);

export function getClaimPlayerZincRewardsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_PLAYER_ZINC_REWARDS_DISCRIMINATOR,
  );
}

export type ClaimPlayerZincRewardsInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountZincMint extends string | AccountMeta<string> = string,
  TAccountPlayerProfile extends string | AccountMeta<string> = string,
  TAccountRoundZincRewardTokenAccount extends string | AccountMeta<string> =
    string,
  TAccountSignerZincTokenAccount extends string | AccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountZincMint extends string
        ? ReadonlyAccount<TAccountZincMint>
        : TAccountZincMint,
      TAccountPlayerProfile extends string
        ? WritableAccount<TAccountPlayerProfile>
        : TAccountPlayerProfile,
      TAccountRoundZincRewardTokenAccount extends string
        ? WritableAccount<TAccountRoundZincRewardTokenAccount>
        : TAccountRoundZincRewardTokenAccount,
      TAccountSignerZincTokenAccount extends string
        ? WritableAccount<TAccountSignerZincTokenAccount>
        : TAccountSignerZincTokenAccount,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimPlayerZincRewardsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClaimPlayerZincRewardsInstructionDataArgs = {};

export function getClaimPlayerZincRewardsInstructionDataEncoder(): FixedSizeEncoder<ClaimPlayerZincRewardsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLAIM_PLAYER_ZINC_REWARDS_DISCRIMINATOR,
    }),
  );
}

export function getClaimPlayerZincRewardsInstructionDataDecoder(): FixedSizeDecoder<ClaimPlayerZincRewardsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimPlayerZincRewardsInstructionDataCodec(): FixedSizeCodec<
  ClaimPlayerZincRewardsInstructionDataArgs,
  ClaimPlayerZincRewardsInstructionData
> {
  return combineCodec(
    getClaimPlayerZincRewardsInstructionDataEncoder(),
    getClaimPlayerZincRewardsInstructionDataDecoder(),
  );
}

export type ClaimPlayerZincRewardsAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountZincMint extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountRoundZincRewardTokenAccount extends string = string,
  TAccountSignerZincTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Player signer that owns the profile reward ledger and receives ZINC. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the live round ZINC claim fee. */
  config?: Address<TAccountConfig>;
  /** Treasury PDA that owns the singleton round-reward vault. */
  treasury?: Address<TAccountTreasury>;
  /** Protocol ZINC mint. */
  zincMint: Address<TAccountZincMint>;
  /** Player profile holding aggregate round ZINC reward accounting. */
  playerProfile?: Address<TAccountPlayerProfile>;
  /** Treasury-owned reward vault that funds aggregate round ZINC claims. */
  roundZincRewardTokenAccount?: Address<TAccountRoundZincRewardTokenAccount>;
  /** Signer-owned destination account for claimed ZINC rewards. */
  signerZincTokenAccount?: Address<TAccountSignerZincTokenAccount>;
  /** Associated Token Program used to create the signer ATA on demand. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the reward vault and ZINC mint. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used if the signer ATA needs to be created. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getClaimPlayerZincRewardsInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountZincMint extends string,
  TAccountPlayerProfile extends string,
  TAccountRoundZincRewardTokenAccount extends string,
  TAccountSignerZincTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: ClaimPlayerZincRewardsAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountZincMint,
    TAccountPlayerProfile,
    TAccountRoundZincRewardTokenAccount,
    TAccountSignerZincTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimPlayerZincRewardsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountZincMint,
    TAccountPlayerProfile,
    TAccountRoundZincRewardTokenAccount,
    TAccountSignerZincTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    zincMint: { value: input.zincMint ?? null, isWritable: false },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    roundZincRewardTokenAccount: {
      value: input.roundZincRewardTokenAccount ?? null,
      isWritable: true,
    },
    signerZincTokenAccount: {
      value: input.signerZincTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.playerProfile.value) {
    accounts.playerProfile.value = await findPlayerProfilePda({
      signer: getAddressFromResolvedInstructionAccount(
        "signer",
        accounts.signer.value,
      ),
    });
  }
  if (!accounts.roundZincRewardTokenAccount.value) {
    accounts.roundZincRewardTokenAccount.value =
      await findRoundZincRewardTokenAccountPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.signerZincTokenAccount.value) {
    accounts.signerZincTokenAccount.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "signer",
            accounts.signer.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "tokenProgram",
            accounts.tokenProgram.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "zincMint",
            accounts.zincMint.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("zincMint", accounts.zincMint),
      getAccountMeta("playerProfile", accounts.playerProfile),
      getAccountMeta(
        "roundZincRewardTokenAccount",
        accounts.roundZincRewardTokenAccount,
      ),
      getAccountMeta("signerZincTokenAccount", accounts.signerZincTokenAccount),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClaimPlayerZincRewardsInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimPlayerZincRewardsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountZincMint,
    TAccountPlayerProfile,
    TAccountRoundZincRewardTokenAccount,
    TAccountSignerZincTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ClaimPlayerZincRewardsInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountZincMint extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountRoundZincRewardTokenAccount extends string = string,
  TAccountSignerZincTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Player signer that owns the profile reward ledger and receives ZINC. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the live round ZINC claim fee. */
  config: Address<TAccountConfig>;
  /** Treasury PDA that owns the singleton round-reward vault. */
  treasury: Address<TAccountTreasury>;
  /** Protocol ZINC mint. */
  zincMint: Address<TAccountZincMint>;
  /** Player profile holding aggregate round ZINC reward accounting. */
  playerProfile: Address<TAccountPlayerProfile>;
  /** Treasury-owned reward vault that funds aggregate round ZINC claims. */
  roundZincRewardTokenAccount: Address<TAccountRoundZincRewardTokenAccount>;
  /** Signer-owned destination account for claimed ZINC rewards. */
  signerZincTokenAccount: Address<TAccountSignerZincTokenAccount>;
  /** Associated Token Program used to create the signer ATA on demand. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the reward vault and ZINC mint. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used if the signer ATA needs to be created. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClaimPlayerZincRewardsInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountZincMint extends string,
  TAccountPlayerProfile extends string,
  TAccountRoundZincRewardTokenAccount extends string,
  TAccountSignerZincTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: ClaimPlayerZincRewardsInput<
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountZincMint,
    TAccountPlayerProfile,
    TAccountRoundZincRewardTokenAccount,
    TAccountSignerZincTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimPlayerZincRewardsInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountTreasury,
  TAccountZincMint,
  TAccountPlayerProfile,
  TAccountRoundZincRewardTokenAccount,
  TAccountSignerZincTokenAccount,
  TAccountAssociatedTokenProgram,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    zincMint: { value: input.zincMint ?? null, isWritable: false },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    roundZincRewardTokenAccount: {
      value: input.roundZincRewardTokenAccount ?? null,
      isWritable: true,
    },
    signerZincTokenAccount: {
      value: input.signerZincTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("zincMint", accounts.zincMint),
      getAccountMeta("playerProfile", accounts.playerProfile),
      getAccountMeta(
        "roundZincRewardTokenAccount",
        accounts.roundZincRewardTokenAccount,
      ),
      getAccountMeta("signerZincTokenAccount", accounts.signerZincTokenAccount),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClaimPlayerZincRewardsInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimPlayerZincRewardsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasury,
    TAccountZincMint,
    TAccountPlayerProfile,
    TAccountRoundZincRewardTokenAccount,
    TAccountSignerZincTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedClaimPlayerZincRewardsInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Player signer that owns the profile reward ledger and receives ZINC. */
    signer: TAccountMetas[0];
    /** Global config containing the live round ZINC claim fee. */
    config: TAccountMetas[1];
    /** Treasury PDA that owns the singleton round-reward vault. */
    treasury: TAccountMetas[2];
    /** Protocol ZINC mint. */
    zincMint: TAccountMetas[3];
    /** Player profile holding aggregate round ZINC reward accounting. */
    playerProfile: TAccountMetas[4];
    /** Treasury-owned reward vault that funds aggregate round ZINC claims. */
    roundZincRewardTokenAccount: TAccountMetas[5];
    /** Signer-owned destination account for claimed ZINC rewards. */
    signerZincTokenAccount: TAccountMetas[6];
    /** Associated Token Program used to create the signer ATA on demand. */
    associatedTokenProgram: TAccountMetas[7];
    /** SPL Token Program that owns the reward vault and ZINC mint. */
    tokenProgram: TAccountMetas[8];
    /** System Program used if the signer ATA needs to be created. */
    systemProgram: TAccountMetas[9];
  };
  data: ClaimPlayerZincRewardsInstructionData;
};

export function parseClaimPlayerZincRewardsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimPlayerZincRewardsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      treasury: getNextAccount(),
      zincMint: getNextAccount(),
      playerProfile: getNextAccount(),
      roundZincRewardTokenAccount: getNextAccount(),
      signerZincTokenAccount: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimPlayerZincRewardsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
