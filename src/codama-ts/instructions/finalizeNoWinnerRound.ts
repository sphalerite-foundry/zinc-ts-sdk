/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findBoardPda,
  findConfigPda,
  findStockpileSolVaultPda,
  findTreasuryPda,
} from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_NO_WINNER_ROUND_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([208, 203, 217, 27, 206, 70, 45, 36]);

export function getFinalizeNoWinnerRoundDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_NO_WINNER_ROUND_DISCRIMINATOR
  );
}

export type FinalizeNoWinnerRoundInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountStockpileSolVault extends string | AccountMeta<string> = string,
  TAccountZincMint extends string | AccountMeta<string> = string,
  TAccountRoundZincPayoutTokenAccount extends
    | string
    | AccountMeta<string> = string,
  TAccountBonanzaTokenAccount extends string | AccountMeta<string> = string,
  TAccountStockpileTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends
    | string
    | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBoard extends string
        ? ReadonlyAccount<TAccountBoard>
        : TAccountBoard,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountStockpileSolVault extends string
        ? WritableAccount<TAccountStockpileSolVault>
        : TAccountStockpileSolVault,
      TAccountZincMint extends string
        ? WritableAccount<TAccountZincMint>
        : TAccountZincMint,
      TAccountRoundZincPayoutTokenAccount extends string
        ? WritableAccount<TAccountRoundZincPayoutTokenAccount>
        : TAccountRoundZincPayoutTokenAccount,
      TAccountBonanzaTokenAccount extends string
        ? WritableAccount<TAccountBonanzaTokenAccount>
        : TAccountBonanzaTokenAccount,
      TAccountStockpileTokenAccount extends string
        ? WritableAccount<TAccountStockpileTokenAccount>
        : TAccountStockpileTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts
    ]
  >;

export type FinalizeNoWinnerRoundInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeNoWinnerRoundInstructionDataArgs = {};

export function getFinalizeNoWinnerRoundInstructionDataEncoder(): FixedSizeEncoder<FinalizeNoWinnerRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: FINALIZE_NO_WINNER_ROUND_DISCRIMINATOR,
    })
  );
}

export function getFinalizeNoWinnerRoundInstructionDataDecoder(): FixedSizeDecoder<FinalizeNoWinnerRoundInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeNoWinnerRoundInstructionDataCodec(): FixedSizeCodec<
  FinalizeNoWinnerRoundInstructionDataArgs,
  FinalizeNoWinnerRoundInstructionData
> {
  return combineCodec(
    getFinalizeNoWinnerRoundInstructionDataEncoder(),
    getFinalizeNoWinnerRoundInstructionDataDecoder()
  );
}

export type FinalizeNoWinnerRoundAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountTreasury extends string = string,
  TAccountStockpileSolVault extends string = string,
  TAccountZincMint extends string = string,
  TAccountRoundZincPayoutTokenAccount extends string = string,
  TAccountBonanzaTokenAccount extends string = string,
  TAccountStockpileTokenAccount extends string = string,
  TAccountTokenProgram extends string = string
> = {
  /** Crank authorized to finalize a zero-winner round. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority and treasury address. */
  config?: Address<TAccountConfig>;
  /** Board authority source for crank checks. */
  board?: Address<TAccountBoard>;
  /** Settled round waiting on zero-winner redirect finalization. */
  round: Address<TAccountRound>;
  /** Treasury signer and accounting state updated by the redirect. */
  treasury?: Address<TAccountTreasury>;
  /** Program-owned lamport vault that accumulates stockpile SOL across rounds. */
  stockpileSolVault?: Address<TAccountStockpileSolVault>;
  /** Protocol ZINC mint used for the no-winner redirect transfer. */
  zincMint: Address<TAccountZincMint>;
  /** Round-owned ZINC vault that funds the no-winner redirect. */
  roundZincPayoutTokenAccount: Address<TAccountRoundZincPayoutTokenAccount>;
  /** Treasury-owned Bonanza vault that receives the 70% redirect share. */
  bonanzaTokenAccount: Address<TAccountBonanzaTokenAccount>;
  /** Treasury-owned stockpile ZINC vault that receives the 30% redirect share. */
  stockpileTokenAccount: Address<TAccountStockpileTokenAccount>;
  /** SPL Token program used for the direct-winner redirect transfer. */
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getFinalizeNoWinnerRoundInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountTreasury extends string,
  TAccountStockpileSolVault extends string,
  TAccountZincMint extends string,
  TAccountRoundZincPayoutTokenAccount extends string,
  TAccountBonanzaTokenAccount extends string,
  TAccountStockpileTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: FinalizeNoWinnerRoundAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountZincMint,
    TAccountRoundZincPayoutTokenAccount,
    TAccountBonanzaTokenAccount,
    TAccountStockpileTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  FinalizeNoWinnerRoundInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountZincMint,
    TAccountRoundZincPayoutTokenAccount,
    TAccountBonanzaTokenAccount,
    TAccountStockpileTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    stockpileSolVault: {
      value: input.stockpileSolVault ?? null,
      isWritable: true,
    },
    zincMint: { value: input.zincMint ?? null, isWritable: true },
    roundZincPayoutTokenAccount: {
      value: input.roundZincPayoutTokenAccount ?? null,
      isWritable: true,
    },
    bonanzaTokenAccount: {
      value: input.bonanzaTokenAccount ?? null,
      isWritable: true,
    },
    stockpileTokenAccount: {
      value: input.stockpileTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.stockpileSolVault.value) {
    accounts.stockpileSolVault.value = await findStockpileSolVaultPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("stockpileSolVault", accounts.stockpileSolVault),
      getAccountMeta("zincMint", accounts.zincMint),
      getAccountMeta(
        "roundZincPayoutTokenAccount",
        accounts.roundZincPayoutTokenAccount
      ),
      getAccountMeta("bonanzaTokenAccount", accounts.bonanzaTokenAccount),
      getAccountMeta("stockpileTokenAccount", accounts.stockpileTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getFinalizeNoWinnerRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeNoWinnerRoundInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountBoard, TAccountRound, TAccountTreasury, TAccountStockpileSolVault, TAccountZincMint, TAccountRoundZincPayoutTokenAccount, TAccountBonanzaTokenAccount, TAccountStockpileTokenAccount, TAccountTokenProgram>);
}

export type FinalizeNoWinnerRoundInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountTreasury extends string = string,
  TAccountStockpileSolVault extends string = string,
  TAccountZincMint extends string = string,
  TAccountRoundZincPayoutTokenAccount extends string = string,
  TAccountBonanzaTokenAccount extends string = string,
  TAccountStockpileTokenAccount extends string = string,
  TAccountTokenProgram extends string = string
> = {
  /** Crank authorized to finalize a zero-winner round. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority and treasury address. */
  config: Address<TAccountConfig>;
  /** Board authority source for crank checks. */
  board: Address<TAccountBoard>;
  /** Settled round waiting on zero-winner redirect finalization. */
  round: Address<TAccountRound>;
  /** Treasury signer and accounting state updated by the redirect. */
  treasury: Address<TAccountTreasury>;
  /** Program-owned lamport vault that accumulates stockpile SOL across rounds. */
  stockpileSolVault: Address<TAccountStockpileSolVault>;
  /** Protocol ZINC mint used for the no-winner redirect transfer. */
  zincMint: Address<TAccountZincMint>;
  /** Round-owned ZINC vault that funds the no-winner redirect. */
  roundZincPayoutTokenAccount: Address<TAccountRoundZincPayoutTokenAccount>;
  /** Treasury-owned Bonanza vault that receives the 70% redirect share. */
  bonanzaTokenAccount: Address<TAccountBonanzaTokenAccount>;
  /** Treasury-owned stockpile ZINC vault that receives the 30% redirect share. */
  stockpileTokenAccount: Address<TAccountStockpileTokenAccount>;
  /** SPL Token program used for the direct-winner redirect transfer. */
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getFinalizeNoWinnerRoundInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountTreasury extends string,
  TAccountStockpileSolVault extends string,
  TAccountZincMint extends string,
  TAccountRoundZincPayoutTokenAccount extends string,
  TAccountBonanzaTokenAccount extends string,
  TAccountStockpileTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: FinalizeNoWinnerRoundInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountTreasury,
    TAccountStockpileSolVault,
    TAccountZincMint,
    TAccountRoundZincPayoutTokenAccount,
    TAccountBonanzaTokenAccount,
    TAccountStockpileTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): FinalizeNoWinnerRoundInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountBoard,
  TAccountRound,
  TAccountTreasury,
  TAccountStockpileSolVault,
  TAccountZincMint,
  TAccountRoundZincPayoutTokenAccount,
  TAccountBonanzaTokenAccount,
  TAccountStockpileTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    stockpileSolVault: {
      value: input.stockpileSolVault ?? null,
      isWritable: true,
    },
    zincMint: { value: input.zincMint ?? null, isWritable: true },
    roundZincPayoutTokenAccount: {
      value: input.roundZincPayoutTokenAccount ?? null,
      isWritable: true,
    },
    bonanzaTokenAccount: {
      value: input.bonanzaTokenAccount ?? null,
      isWritable: true,
    },
    stockpileTokenAccount: {
      value: input.stockpileTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("stockpileSolVault", accounts.stockpileSolVault),
      getAccountMeta("zincMint", accounts.zincMint),
      getAccountMeta(
        "roundZincPayoutTokenAccount",
        accounts.roundZincPayoutTokenAccount
      ),
      getAccountMeta("bonanzaTokenAccount", accounts.bonanzaTokenAccount),
      getAccountMeta("stockpileTokenAccount", accounts.stockpileTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getFinalizeNoWinnerRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeNoWinnerRoundInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountBoard, TAccountRound, TAccountTreasury, TAccountStockpileSolVault, TAccountZincMint, TAccountRoundZincPayoutTokenAccount, TAccountBonanzaTokenAccount, TAccountStockpileTokenAccount, TAccountTokenProgram>);
}

export type ParsedFinalizeNoWinnerRoundInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank authorized to finalize a zero-winner round. */
    signer: TAccountMetas[0];
    /** Global config containing the crank authority and treasury address. */
    config: TAccountMetas[1];
    /** Board authority source for crank checks. */
    board: TAccountMetas[2];
    /** Settled round waiting on zero-winner redirect finalization. */
    round: TAccountMetas[3];
    /** Treasury signer and accounting state updated by the redirect. */
    treasury: TAccountMetas[4];
    /** Program-owned lamport vault that accumulates stockpile SOL across rounds. */
    stockpileSolVault: TAccountMetas[5];
    /** Protocol ZINC mint used for the no-winner redirect transfer. */
    zincMint: TAccountMetas[6];
    /** Round-owned ZINC vault that funds the no-winner redirect. */
    roundZincPayoutTokenAccount: TAccountMetas[7];
    /** Treasury-owned Bonanza vault that receives the 70% redirect share. */
    bonanzaTokenAccount: TAccountMetas[8];
    /** Treasury-owned stockpile ZINC vault that receives the 30% redirect share. */
    stockpileTokenAccount: TAccountMetas[9];
    /** SPL Token program used for the direct-winner redirect transfer. */
    tokenProgram: TAccountMetas[10];
  };
  data: FinalizeNoWinnerRoundInstructionData;
};

export function parseFinalizeNoWinnerRoundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[]
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFinalizeNoWinnerRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 11,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      board: getNextAccount(),
      round: getNextAccount(),
      treasury: getNextAccount(),
      stockpileSolVault: getNextAccount(),
      zincMint: getNextAccount(),
      roundZincPayoutTokenAccount: getNextAccount(),
      bonanzaTokenAccount: getNextAccount(),
      stockpileTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getFinalizeNoWinnerRoundInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
