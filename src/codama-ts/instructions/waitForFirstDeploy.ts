/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBoardPda, findConfigPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const WAIT_FOR_FIRST_DEPLOY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([119, 118, 139, 250, 239, 205, 246, 157]);

export function getWaitForFirstDeployDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WAIT_FOR_FIRST_DEPLOY_DISCRIMINATOR
  );
}

export type WaitForFirstDeployInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBoard extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountRoundSecret extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBoard extends string
        ? WritableAccount<TAccountBoard>
        : TAccountBoard,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountRoundSecret extends string
        ? ReadonlyAccount<TAccountRoundSecret>
        : TAccountRoundSecret,
      ...TRemainingAccounts
    ]
  >;

export type WaitForFirstDeployInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type WaitForFirstDeployInstructionDataArgs = {};

export function getWaitForFirstDeployInstructionDataEncoder(): FixedSizeEncoder<WaitForFirstDeployInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: WAIT_FOR_FIRST_DEPLOY_DISCRIMINATOR,
    })
  );
}

export function getWaitForFirstDeployInstructionDataDecoder(): FixedSizeDecoder<WaitForFirstDeployInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getWaitForFirstDeployInstructionDataCodec(): FixedSizeCodec<
  WaitForFirstDeployInstructionDataArgs,
  WaitForFirstDeployInstructionData
> {
  return combineCodec(
    getWaitForFirstDeployInstructionDataEncoder(),
    getWaitForFirstDeployInstructionDataDecoder()
  );
}

export type WaitForFirstDeployAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountRoundSecret extends string = string
> = {
  /** Crank authorized to promote prepared rounds into the waiting state. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority. */
  config?: Address<TAccountConfig>;
  /** Global board pointers for the playable waiting round and active round, if any. */
  board?: Address<TAccountBoard>;
  /** Ready round that will become the next playable waiting round. */
  round: Address<TAccountRound>;
  /** Secret account that must already contain the encrypted round random value. */
  roundSecret: Address<TAccountRoundSecret>;
};

export async function getWaitForFirstDeployInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountRoundSecret extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: WaitForFirstDeployAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountRoundSecret
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  WaitForFirstDeployInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountRoundSecret
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundSecret: { value: input.roundSecret ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.board.value) {
    accounts.board.value = await findBoardPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("roundSecret", accounts.roundSecret),
    ],
    data: getWaitForFirstDeployInstructionDataEncoder().encode({}),
    programAddress,
  } as WaitForFirstDeployInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountBoard, TAccountRound, TAccountRoundSecret>);
}

export type WaitForFirstDeployInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountBoard extends string = string,
  TAccountRound extends string = string,
  TAccountRoundSecret extends string = string
> = {
  /** Crank authorized to promote prepared rounds into the waiting state. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the crank authority. */
  config: Address<TAccountConfig>;
  /** Global board pointers for the playable waiting round and active round, if any. */
  board: Address<TAccountBoard>;
  /** Ready round that will become the next playable waiting round. */
  round: Address<TAccountRound>;
  /** Secret account that must already contain the encrypted round random value. */
  roundSecret: Address<TAccountRoundSecret>;
};

export function getWaitForFirstDeployInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountBoard extends string,
  TAccountRound extends string,
  TAccountRoundSecret extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: WaitForFirstDeployInput<
    TAccountSigner,
    TAccountConfig,
    TAccountBoard,
    TAccountRound,
    TAccountRoundSecret
  >,
  config?: { programAddress?: TProgramAddress }
): WaitForFirstDeployInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountBoard,
  TAccountRound,
  TAccountRoundSecret
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    board: { value: input.board ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundSecret: { value: input.roundSecret ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("board", accounts.board),
      getAccountMeta("round", accounts.round),
      getAccountMeta("roundSecret", accounts.roundSecret),
    ],
    data: getWaitForFirstDeployInstructionDataEncoder().encode({}),
    programAddress,
  } as WaitForFirstDeployInstruction<TProgramAddress, TAccountSigner, TAccountConfig, TAccountBoard, TAccountRound, TAccountRoundSecret>);
}

export type ParsedWaitForFirstDeployInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank authorized to promote prepared rounds into the waiting state. */
    signer: TAccountMetas[0];
    /** Global config containing the crank authority. */
    config: TAccountMetas[1];
    /** Global board pointers for the playable waiting round and active round, if any. */
    board: TAccountMetas[2];
    /** Ready round that will become the next playable waiting round. */
    round: TAccountMetas[3];
    /** Secret account that must already contain the encrypted round random value. */
    roundSecret: TAccountMetas[4];
  };
  data: WaitForFirstDeployInstructionData;
};

export function parseWaitForFirstDeployInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[]
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWaitForFirstDeployInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      board: getNextAccount(),
      round: getNextAccount(),
      roundSecret: getNextAccount(),
    },
    data: getWaitForFirstDeployInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
