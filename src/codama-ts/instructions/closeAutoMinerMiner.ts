/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_AUTO_MINER_MINER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([249, 242, 200, 76, 176, 8, 241, 177]);

export function getCloseAutoMinerMinerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_AUTO_MINER_MINER_DISCRIMINATOR,
  );
}

export type CloseAutoMinerMinerInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountAutoMinerSession extends string | AccountMeta<string> = string,
  TAccountMiner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountAutoMinerSession extends string
        ? WritableAccount<TAccountAutoMinerSession>
        : TAccountAutoMinerSession,
      TAccountMiner extends string
        ? WritableAccount<TAccountMiner>
        : TAccountMiner,
      ...TRemainingAccounts,
    ]
  >;

export type CloseAutoMinerMinerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseAutoMinerMinerInstructionDataArgs = {};

export function getCloseAutoMinerMinerInstructionDataEncoder(): FixedSizeEncoder<CloseAutoMinerMinerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_AUTO_MINER_MINER_DISCRIMINATOR,
    }),
  );
}

export function getCloseAutoMinerMinerInstructionDataDecoder(): FixedSizeDecoder<CloseAutoMinerMinerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseAutoMinerMinerInstructionDataCodec(): FixedSizeCodec<
  CloseAutoMinerMinerInstructionDataArgs,
  CloseAutoMinerMinerInstructionData
> {
  return combineCodec(
    getCloseAutoMinerMinerInstructionDataEncoder(),
    getCloseAutoMinerMinerInstructionDataDecoder(),
  );
}

export type CloseAutoMinerMinerAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountMiner extends string = string,
> = {
  /** Crank signer authorized to submit cleanup transactions. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the cleanup crank authority. */
  config?: Address<TAccountConfig>;
  /** Terminal round whose miner cleanup counter is advanced. */
  round: Address<TAccountRound>;
  /** Auto-miner session that created this miner and receives reclaimed rent. */
  autoMinerSession: Address<TAccountAutoMinerSession>;
  /** Settled auto-created miner account being closed to its session. */
  miner: Address<TAccountMiner>;
};

export async function getCloseAutoMinerMinerInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TAccountAutoMinerSession extends string,
  TAccountMiner extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseAutoMinerMinerAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountRound,
    TAccountAutoMinerSession,
    TAccountMiner
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseAutoMinerMinerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountRound,
    TAccountAutoMinerSession,
    TAccountMiner
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    miner: { value: input.miner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("round", accounts.round),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("miner", accounts.miner),
    ],
    data: getCloseAutoMinerMinerInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseAutoMinerMinerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountRound,
    TAccountAutoMinerSession,
    TAccountMiner
  >);
}

export type CloseAutoMinerMinerInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountRound extends string = string,
  TAccountAutoMinerSession extends string = string,
  TAccountMiner extends string = string,
> = {
  /** Crank signer authorized to submit cleanup transactions. */
  signer: TransactionSigner<TAccountSigner>;
  /** Global config containing the cleanup crank authority. */
  config: Address<TAccountConfig>;
  /** Terminal round whose miner cleanup counter is advanced. */
  round: Address<TAccountRound>;
  /** Auto-miner session that created this miner and receives reclaimed rent. */
  autoMinerSession: Address<TAccountAutoMinerSession>;
  /** Settled auto-created miner account being closed to its session. */
  miner: Address<TAccountMiner>;
};

export function getCloseAutoMinerMinerInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountRound extends string,
  TAccountAutoMinerSession extends string,
  TAccountMiner extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseAutoMinerMinerInput<
    TAccountSigner,
    TAccountConfig,
    TAccountRound,
    TAccountAutoMinerSession,
    TAccountMiner
  >,
  config?: { programAddress?: TProgramAddress },
): CloseAutoMinerMinerInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountRound,
  TAccountAutoMinerSession,
  TAccountMiner
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    autoMinerSession: {
      value: input.autoMinerSession ?? null,
      isWritable: true,
    },
    miner: { value: input.miner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("round", accounts.round),
      getAccountMeta("autoMinerSession", accounts.autoMinerSession),
      getAccountMeta("miner", accounts.miner),
    ],
    data: getCloseAutoMinerMinerInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseAutoMinerMinerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountRound,
    TAccountAutoMinerSession,
    TAccountMiner
  >);
}

export type ParsedCloseAutoMinerMinerInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Crank signer authorized to submit cleanup transactions. */
    signer: TAccountMetas[0];
    /** Global config containing the cleanup crank authority. */
    config: TAccountMetas[1];
    /** Terminal round whose miner cleanup counter is advanced. */
    round: TAccountMetas[2];
    /** Auto-miner session that created this miner and receives reclaimed rent. */
    autoMinerSession: TAccountMetas[3];
    /** Settled auto-created miner account being closed to its session. */
    miner: TAccountMetas[4];
  };
  data: CloseAutoMinerMinerInstructionData;
};

export function parseCloseAutoMinerMinerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseAutoMinerMinerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      round: getNextAccount(),
      autoMinerSession: getNextAccount(),
      miner: getNextAccount(),
    },
    data: getCloseAutoMinerMinerInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
