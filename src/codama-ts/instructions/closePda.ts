/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_PDA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  42, 10, 115, 220, 35, 21, 218, 96,
]);

export function getClosePdaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_PDA_DISCRIMINATOR);
}

export type ClosePdaInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountAccount extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountAccount extends string
        ? WritableAccount<TAccountAccount>
        : TAccountAccount,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts
    ]
  >;

export type ClosePdaInstructionData = {
  discriminator: ReadonlyUint8Array;
  allowProtectedClose: boolean;
};

export type ClosePdaInstructionDataArgs = { allowProtectedClose: boolean };

export function getClosePdaInstructionDataEncoder(): FixedSizeEncoder<ClosePdaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["allowProtectedClose", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_PDA_DISCRIMINATOR })
  );
}

export function getClosePdaInstructionDataDecoder(): FixedSizeDecoder<ClosePdaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["allowProtectedClose", getBooleanDecoder()],
  ]);
}

export function getClosePdaInstructionDataCodec(): FixedSizeCodec<
  ClosePdaInstructionDataArgs,
  ClosePdaInstructionData
> {
  return combineCodec(
    getClosePdaInstructionDataEncoder(),
    getClosePdaInstructionDataDecoder()
  );
}

export type ClosePdaAsyncInput<
  TAccountAdmin extends string = string,
  TAccountAccount extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string
> = {
  admin: TransactionSigner<TAccountAdmin>;
  account: Address<TAccountAccount>;
  config?: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  allowProtectedClose: ClosePdaInstructionDataArgs["allowProtectedClose"];
};

export async function getClosePdaInstructionAsync<
  TAccountAdmin extends string,
  TAccountAccount extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: ClosePdaAsyncInput<
    TAccountAdmin,
    TAccountAccount,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClosePdaInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountAccount,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    account: { value: input.account ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("account", accounts.account),
      getAccountMeta("config", accounts.config),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClosePdaInstructionDataEncoder().encode(
      args as ClosePdaInstructionDataArgs
    ),
    programAddress,
  } as ClosePdaInstruction<TProgramAddress, TAccountAdmin, TAccountAccount, TAccountConfig, TAccountSystemProgram>);
}

export type ClosePdaInput<
  TAccountAdmin extends string = string,
  TAccountAccount extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string
> = {
  admin: TransactionSigner<TAccountAdmin>;
  account: Address<TAccountAccount>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  allowProtectedClose: ClosePdaInstructionDataArgs["allowProtectedClose"];
};

export function getClosePdaInstruction<
  TAccountAdmin extends string,
  TAccountAccount extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: ClosePdaInput<
    TAccountAdmin,
    TAccountAccount,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ClosePdaInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountAccount,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    account: { value: input.account ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("account", accounts.account),
      getAccountMeta("config", accounts.config),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClosePdaInstructionDataEncoder().encode(
      args as ClosePdaInstructionDataArgs
    ),
    programAddress,
  } as ClosePdaInstruction<TProgramAddress, TAccountAdmin, TAccountAccount, TAccountConfig, TAccountSystemProgram>);
}

export type ParsedClosePdaInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    account: TAccountMetas[1];
    config: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ClosePdaInstructionData;
};

export function parseClosePdaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[]
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClosePdaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      account: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClosePdaInstructionDataDecoder().decode(instruction.data),
  };
}
