/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";
import {
  getSignedComputationOutputsRevealRoundRandOutputDecoder,
  getSignedComputationOutputsRevealRoundRandOutputEncoder,
  type SignedComputationOutputsRevealRoundRandOutput,
  type SignedComputationOutputsRevealRoundRandOutputArgs,
} from "../types";

export const REVEAL_ROUND_RAND_CALLBACK_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([49, 83, 220, 33, 54, 155, 235, 114]);

export function getRevealRoundRandCallbackDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVEAL_ROUND_RAND_CALLBACK_DISCRIMINATOR
  );
}

export type RevealRoundRandCallbackInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountArciumProgram extends
    | string
    | AccountMeta<string> = "Arcj82pX7HxYKLR92qvgZUAd7vGS1k4hQvAFcPATFdEQ",
  TAccountCompDefAccount extends string | AccountMeta<string> = string,
  TAccountMxeAccount extends string | AccountMeta<string> = string,
  TAccountComputationAccount extends string | AccountMeta<string> = string,
  TAccountClusterAccount extends string | AccountMeta<string> = string,
  TAccountInstructionsSysvar extends
    | string
    | AccountMeta<string> = "Sysvar1nstructions1111111111111111111111111",
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountArciumProgram extends string
        ? ReadonlyAccount<TAccountArciumProgram>
        : TAccountArciumProgram,
      TAccountCompDefAccount extends string
        ? ReadonlyAccount<TAccountCompDefAccount>
        : TAccountCompDefAccount,
      TAccountMxeAccount extends string
        ? ReadonlyAccount<TAccountMxeAccount>
        : TAccountMxeAccount,
      TAccountComputationAccount extends string
        ? ReadonlyAccount<TAccountComputationAccount>
        : TAccountComputationAccount,
      TAccountClusterAccount extends string
        ? ReadonlyAccount<TAccountClusterAccount>
        : TAccountClusterAccount,
      TAccountInstructionsSysvar extends string
        ? ReadonlyAccount<TAccountInstructionsSysvar>
        : TAccountInstructionsSysvar,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts
    ]
  >;

export type RevealRoundRandCallbackInstructionData = {
  discriminator: ReadonlyUint8Array;
  output: SignedComputationOutputsRevealRoundRandOutput;
};

export type RevealRoundRandCallbackInstructionDataArgs = {
  output: SignedComputationOutputsRevealRoundRandOutputArgs;
};

export function getRevealRoundRandCallbackInstructionDataEncoder(): Encoder<RevealRoundRandCallbackInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["output", getSignedComputationOutputsRevealRoundRandOutputEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REVEAL_ROUND_RAND_CALLBACK_DISCRIMINATOR,
    })
  );
}

export function getRevealRoundRandCallbackInstructionDataDecoder(): Decoder<RevealRoundRandCallbackInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["output", getSignedComputationOutputsRevealRoundRandOutputDecoder()],
  ]);
}

export function getRevealRoundRandCallbackInstructionDataCodec(): Codec<
  RevealRoundRandCallbackInstructionDataArgs,
  RevealRoundRandCallbackInstructionData
> {
  return combineCodec(
    getRevealRoundRandCallbackInstructionDataEncoder(),
    getRevealRoundRandCallbackInstructionDataDecoder()
  );
}

export type RevealRoundRandCallbackAsyncInput<
  TAccountArciumProgram extends string = string,
  TAccountCompDefAccount extends string = string,
  TAccountMxeAccount extends string = string,
  TAccountComputationAccount extends string = string,
  TAccountClusterAccount extends string = string,
  TAccountInstructionsSysvar extends string = string,
  TAccountRound extends string = string,
  TAccountConfig extends string = string
> = {
  arciumProgram?: Address<TAccountArciumProgram>;
  /** Computation definition used to verify the callback output. */
  compDefAccount: Address<TAccountCompDefAccount>;
  /** MXE account backing the encrypted computations for this program. */
  mxeAccount: Address<TAccountMxeAccount>;
  computationAccount: Address<TAccountComputationAccount>;
  /** Arcium cluster state used to verify the signed output. */
  clusterAccount: Address<TAccountClusterAccount>;
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  /** Round being settled after Arcium reveals its random value. */
  round: Address<TAccountRound>;
  /** Live config used to resolve Wildcat eligibility at reveal time. */
  config?: Address<TAccountConfig>;
  output: RevealRoundRandCallbackInstructionDataArgs["output"];
};

export async function getRevealRoundRandCallbackInstructionAsync<
  TAccountArciumProgram extends string,
  TAccountCompDefAccount extends string,
  TAccountMxeAccount extends string,
  TAccountComputationAccount extends string,
  TAccountClusterAccount extends string,
  TAccountInstructionsSysvar extends string,
  TAccountRound extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: RevealRoundRandCallbackAsyncInput<
    TAccountArciumProgram,
    TAccountCompDefAccount,
    TAccountMxeAccount,
    TAccountComputationAccount,
    TAccountClusterAccount,
    TAccountInstructionsSysvar,
    TAccountRound,
    TAccountConfig
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RevealRoundRandCallbackInstruction<
    TProgramAddress,
    TAccountArciumProgram,
    TAccountCompDefAccount,
    TAccountMxeAccount,
    TAccountComputationAccount,
    TAccountClusterAccount,
    TAccountInstructionsSysvar,
    TAccountRound,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    arciumProgram: { value: input.arciumProgram ?? null, isWritable: false },
    compDefAccount: { value: input.compDefAccount ?? null, isWritable: false },
    mxeAccount: { value: input.mxeAccount ?? null, isWritable: false },
    computationAccount: {
      value: input.computationAccount ?? null,
      isWritable: false,
    },
    clusterAccount: { value: input.clusterAccount ?? null, isWritable: false },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
    round: { value: input.round ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.arciumProgram.value) {
    accounts.arciumProgram.value =
      "Arcj82pX7HxYKLR92qvgZUAd7vGS1k4hQvAFcPATFdEQ" as Address<"Arcj82pX7HxYKLR92qvgZUAd7vGS1k4hQvAFcPATFdEQ">;
  }
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("arciumProgram", accounts.arciumProgram),
      getAccountMeta("compDefAccount", accounts.compDefAccount),
      getAccountMeta("mxeAccount", accounts.mxeAccount),
      getAccountMeta("computationAccount", accounts.computationAccount),
      getAccountMeta("clusterAccount", accounts.clusterAccount),
      getAccountMeta("instructionsSysvar", accounts.instructionsSysvar),
      getAccountMeta("round", accounts.round),
      getAccountMeta("config", accounts.config),
    ],
    data: getRevealRoundRandCallbackInstructionDataEncoder().encode(
      args as RevealRoundRandCallbackInstructionDataArgs
    ),
    programAddress,
  } as RevealRoundRandCallbackInstruction<TProgramAddress, TAccountArciumProgram, TAccountCompDefAccount, TAccountMxeAccount, TAccountComputationAccount, TAccountClusterAccount, TAccountInstructionsSysvar, TAccountRound, TAccountConfig>);
}

export type RevealRoundRandCallbackInput<
  TAccountArciumProgram extends string = string,
  TAccountCompDefAccount extends string = string,
  TAccountMxeAccount extends string = string,
  TAccountComputationAccount extends string = string,
  TAccountClusterAccount extends string = string,
  TAccountInstructionsSysvar extends string = string,
  TAccountRound extends string = string,
  TAccountConfig extends string = string
> = {
  arciumProgram?: Address<TAccountArciumProgram>;
  /** Computation definition used to verify the callback output. */
  compDefAccount: Address<TAccountCompDefAccount>;
  /** MXE account backing the encrypted computations for this program. */
  mxeAccount: Address<TAccountMxeAccount>;
  computationAccount: Address<TAccountComputationAccount>;
  /** Arcium cluster state used to verify the signed output. */
  clusterAccount: Address<TAccountClusterAccount>;
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  /** Round being settled after Arcium reveals its random value. */
  round: Address<TAccountRound>;
  /** Live config used to resolve Wildcat eligibility at reveal time. */
  config: Address<TAccountConfig>;
  output: RevealRoundRandCallbackInstructionDataArgs["output"];
};

export function getRevealRoundRandCallbackInstruction<
  TAccountArciumProgram extends string,
  TAccountCompDefAccount extends string,
  TAccountMxeAccount extends string,
  TAccountComputationAccount extends string,
  TAccountClusterAccount extends string,
  TAccountInstructionsSysvar extends string,
  TAccountRound extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS
>(
  input: RevealRoundRandCallbackInput<
    TAccountArciumProgram,
    TAccountCompDefAccount,
    TAccountMxeAccount,
    TAccountComputationAccount,
    TAccountClusterAccount,
    TAccountInstructionsSysvar,
    TAccountRound,
    TAccountConfig
  >,
  config?: { programAddress?: TProgramAddress }
): RevealRoundRandCallbackInstruction<
  TProgramAddress,
  TAccountArciumProgram,
  TAccountCompDefAccount,
  TAccountMxeAccount,
  TAccountComputationAccount,
  TAccountClusterAccount,
  TAccountInstructionsSysvar,
  TAccountRound,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    arciumProgram: { value: input.arciumProgram ?? null, isWritable: false },
    compDefAccount: { value: input.compDefAccount ?? null, isWritable: false },
    mxeAccount: { value: input.mxeAccount ?? null, isWritable: false },
    computationAccount: {
      value: input.computationAccount ?? null,
      isWritable: false,
    },
    clusterAccount: { value: input.clusterAccount ?? null, isWritable: false },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
    round: { value: input.round ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.arciumProgram.value) {
    accounts.arciumProgram.value =
      "Arcj82pX7HxYKLR92qvgZUAd7vGS1k4hQvAFcPATFdEQ" as Address<"Arcj82pX7HxYKLR92qvgZUAd7vGS1k4hQvAFcPATFdEQ">;
  }
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("arciumProgram", accounts.arciumProgram),
      getAccountMeta("compDefAccount", accounts.compDefAccount),
      getAccountMeta("mxeAccount", accounts.mxeAccount),
      getAccountMeta("computationAccount", accounts.computationAccount),
      getAccountMeta("clusterAccount", accounts.clusterAccount),
      getAccountMeta("instructionsSysvar", accounts.instructionsSysvar),
      getAccountMeta("round", accounts.round),
      getAccountMeta("config", accounts.config),
    ],
    data: getRevealRoundRandCallbackInstructionDataEncoder().encode(
      args as RevealRoundRandCallbackInstructionDataArgs
    ),
    programAddress,
  } as RevealRoundRandCallbackInstruction<TProgramAddress, TAccountArciumProgram, TAccountCompDefAccount, TAccountMxeAccount, TAccountComputationAccount, TAccountClusterAccount, TAccountInstructionsSysvar, TAccountRound, TAccountConfig>);
}

export type ParsedRevealRoundRandCallbackInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
  programAddress: Address<TProgram>;
  accounts: {
    arciumProgram: TAccountMetas[0];
    /** Computation definition used to verify the callback output. */
    compDefAccount: TAccountMetas[1];
    /** MXE account backing the encrypted computations for this program. */
    mxeAccount: TAccountMetas[2];
    computationAccount: TAccountMetas[3];
    /** Arcium cluster state used to verify the signed output. */
    clusterAccount: TAccountMetas[4];
    instructionsSysvar: TAccountMetas[5];
    /** Round being settled after Arcium reveals its random value. */
    round: TAccountMetas[6];
    /** Live config used to resolve Wildcat eligibility at reveal time. */
    config: TAccountMetas[7];
  };
  data: RevealRoundRandCallbackInstructionData;
};

export function parseRevealRoundRandCallbackInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[]
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRevealRoundRandCallbackInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      arciumProgram: getNextAccount(),
      compDefAccount: getNextAccount(),
      mxeAccount: getNextAccount(),
      computationAccount: getNextAccount(),
      clusterAccount: getNextAccount(),
      instructionsSysvar: getNextAccount(),
      round: getNextAccount(),
      config: getNextAccount(),
    },
    data: getRevealRoundRandCallbackInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
