/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findTreasuryPda } from "../pdas";
import { ZINC_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_ROUND_TOKEN_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([65, 206, 161, 77, 196, 20, 201, 63]);

export function getCloseRoundTokenAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_ROUND_TOKEN_ACCOUNT_DISCRIMINATOR,
  );
}

export type CloseRoundTokenAccountInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountZincMint extends string | AccountMeta<string> = string,
  TAccountSourceTokenAccount extends string | AccountMeta<string> = string,
  TAccountAdminTokenAccount extends string | AccountMeta<string> = string,
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasury extends string
        ? ReadonlyAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountZincMint extends string
        ? ReadonlyAccount<TAccountZincMint>
        : TAccountZincMint,
      TAccountSourceTokenAccount extends string
        ? WritableAccount<TAccountSourceTokenAccount>
        : TAccountSourceTokenAccount,
      TAccountAdminTokenAccount extends string
        ? WritableAccount<TAccountAdminTokenAccount>
        : TAccountAdminTokenAccount,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseRoundTokenAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseRoundTokenAccountInstructionDataArgs = {};

export function getCloseRoundTokenAccountInstructionDataEncoder(): FixedSizeEncoder<CloseRoundTokenAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_ROUND_TOKEN_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

export function getCloseRoundTokenAccountInstructionDataDecoder(): FixedSizeDecoder<CloseRoundTokenAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseRoundTokenAccountInstructionDataCodec(): FixedSizeCodec<
  CloseRoundTokenAccountInstructionDataArgs,
  CloseRoundTokenAccountInstructionData
> {
  return combineCodec(
    getCloseRoundTokenAccountInstructionDataEncoder(),
    getCloseRoundTokenAccountInstructionDataDecoder(),
  );
}

export type CloseRoundTokenAccountAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountRound extends string = string,
  TAccountZincMint extends string = string,
  TAccountSourceTokenAccount extends string = string,
  TAccountAdminTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Admin signer that receives drained tokens and closed-account rent. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Global config that authorizes the admin and treasury relationship. */
  config?: Address<TAccountConfig>;
  /** Treasury PDA used to validate the canonical ZINC mint and payout vault seeds. */
  treasury?: Address<TAccountTreasury>;
  /** Round PDA that owns the source ZINC payout vault. */
  round: Address<TAccountRound>;
  /** Protocol ZINC mint held by the round payout vault. */
  zincMint: Address<TAccountZincMint>;
  /** Round-owned ZINC payout vault to drain and close during admin reset. */
  sourceTokenAccount: Address<TAccountSourceTokenAccount>;
  /** Admin ATA that receives the drained ZINC balance. */
  adminTokenAccount?: Address<TAccountAdminTokenAccount>;
  /** Associated Token Program used to create the admin ATA when absent. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the source and destination token accounts. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used by ATA creation. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCloseRoundTokenAccountInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountRound extends string,
  TAccountZincMint extends string,
  TAccountSourceTokenAccount extends string,
  TAccountAdminTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseRoundTokenAccountAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountRound,
    TAccountZincMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseRoundTokenAccountInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountRound,
    TAccountZincMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    zincMint: { value: input.zincMint ?? null, isWritable: false },
    sourceTokenAccount: {
      value: input.sourceTokenAccount ?? null,
      isWritable: true,
    },
    adminTokenAccount: {
      value: input.adminTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await findTreasuryPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.adminTokenAccount.value) {
    accounts.adminTokenAccount.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "admin",
            accounts.admin.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "tokenProgram",
            accounts.tokenProgram.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "zincMint",
            accounts.zincMint.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("round", accounts.round),
      getAccountMeta("zincMint", accounts.zincMint),
      getAccountMeta("sourceTokenAccount", accounts.sourceTokenAccount),
      getAccountMeta("adminTokenAccount", accounts.adminTokenAccount),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseRoundTokenAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseRoundTokenAccountInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountRound,
    TAccountZincMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type CloseRoundTokenAccountInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountRound extends string = string,
  TAccountZincMint extends string = string,
  TAccountSourceTokenAccount extends string = string,
  TAccountAdminTokenAccount extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Admin signer that receives drained tokens and closed-account rent. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Global config that authorizes the admin and treasury relationship. */
  config: Address<TAccountConfig>;
  /** Treasury PDA used to validate the canonical ZINC mint and payout vault seeds. */
  treasury: Address<TAccountTreasury>;
  /** Round PDA that owns the source ZINC payout vault. */
  round: Address<TAccountRound>;
  /** Protocol ZINC mint held by the round payout vault. */
  zincMint: Address<TAccountZincMint>;
  /** Round-owned ZINC payout vault to drain and close during admin reset. */
  sourceTokenAccount: Address<TAccountSourceTokenAccount>;
  /** Admin ATA that receives the drained ZINC balance. */
  adminTokenAccount: Address<TAccountAdminTokenAccount>;
  /** Associated Token Program used to create the admin ATA when absent. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** SPL Token Program that owns the source and destination token accounts. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** System Program used by ATA creation. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCloseRoundTokenAccountInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountRound extends string,
  TAccountZincMint extends string,
  TAccountSourceTokenAccount extends string,
  TAccountAdminTokenAccount extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZINC_PROGRAM_ADDRESS,
>(
  input: CloseRoundTokenAccountInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountRound,
    TAccountZincMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseRoundTokenAccountInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountTreasury,
  TAccountRound,
  TAccountZincMint,
  TAccountSourceTokenAccount,
  TAccountAdminTokenAccount,
  TAccountAssociatedTokenProgram,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZINC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
    zincMint: { value: input.zincMint ?? null, isWritable: false },
    sourceTokenAccount: {
      value: input.sourceTokenAccount ?? null,
      isWritable: true,
    },
    adminTokenAccount: {
      value: input.adminTokenAccount ?? null,
      isWritable: true,
    },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("round", accounts.round),
      getAccountMeta("zincMint", accounts.zincMint),
      getAccountMeta("sourceTokenAccount", accounts.sourceTokenAccount),
      getAccountMeta("adminTokenAccount", accounts.adminTokenAccount),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseRoundTokenAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseRoundTokenAccountInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountTreasury,
    TAccountRound,
    TAccountZincMint,
    TAccountSourceTokenAccount,
    TAccountAdminTokenAccount,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCloseRoundTokenAccountInstruction<
  TProgram extends string = typeof ZINC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin signer that receives drained tokens and closed-account rent. */
    admin: TAccountMetas[0];
    /** Global config that authorizes the admin and treasury relationship. */
    config: TAccountMetas[1];
    /** Treasury PDA used to validate the canonical ZINC mint and payout vault seeds. */
    treasury: TAccountMetas[2];
    /** Round PDA that owns the source ZINC payout vault. */
    round: TAccountMetas[3];
    /** Protocol ZINC mint held by the round payout vault. */
    zincMint: TAccountMetas[4];
    /** Round-owned ZINC payout vault to drain and close during admin reset. */
    sourceTokenAccount: TAccountMetas[5];
    /** Admin ATA that receives the drained ZINC balance. */
    adminTokenAccount: TAccountMetas[6];
    /** Associated Token Program used to create the admin ATA when absent. */
    associatedTokenProgram: TAccountMetas[7];
    /** SPL Token Program that owns the source and destination token accounts. */
    tokenProgram: TAccountMetas[8];
    /** System Program used by ATA creation. */
    systemProgram: TAccountMetas[9];
  };
  data: CloseRoundTokenAccountInstructionData;
};

export function parseCloseRoundTokenAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseRoundTokenAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      treasury: getNextAccount(),
      round: getNextAccount(),
      zincMint: getNextAccount(),
      sourceTokenAccount: getNextAccount(),
      adminTokenAccount: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseRoundTokenAccountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
