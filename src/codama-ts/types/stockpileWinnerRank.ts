/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/** One ranked Stockpile winner payout slot. */
export type StockpileWinnerRank = {
  /** Selected winner wallet for this rank. */
  winner: Address;
  /** Ticket drawn for this rank in `x10k` units over the then-remaining entry weight. */
  winningTicketX10k: bigint;
  /** Effective basis-point share after unfilled lower ranks roll into rank zero. */
  shareBps: bigint;
  /** SOL lamports reserved for this rank. */
  solPayoutAmount: bigint;
  /** ZINC base units reserved for this rank. */
  zincPayoutAmount: bigint;
  /** Whether the base SOL/ZINC payout has already been sent. */
  basePaid: boolean;
};

export type StockpileWinnerRankArgs = {
  /** Selected winner wallet for this rank. */
  winner: Address;
  /** Ticket drawn for this rank in `x10k` units over the then-remaining entry weight. */
  winningTicketX10k: number | bigint;
  /** Effective basis-point share after unfilled lower ranks roll into rank zero. */
  shareBps: number | bigint;
  /** SOL lamports reserved for this rank. */
  solPayoutAmount: number | bigint;
  /** ZINC base units reserved for this rank. */
  zincPayoutAmount: number | bigint;
  /** Whether the base SOL/ZINC payout has already been sent. */
  basePaid: boolean;
};

export function getStockpileWinnerRankEncoder(): FixedSizeEncoder<StockpileWinnerRankArgs> {
  return getStructEncoder([
    ["winner", getAddressEncoder()],
    ["winningTicketX10k", getU64Encoder()],
    ["shareBps", getU64Encoder()],
    ["solPayoutAmount", getU64Encoder()],
    ["zincPayoutAmount", getU64Encoder()],
    ["basePaid", getBooleanEncoder()],
  ]);
}

export function getStockpileWinnerRankDecoder(): FixedSizeDecoder<StockpileWinnerRank> {
  return getStructDecoder([
    ["winner", getAddressDecoder()],
    ["winningTicketX10k", getU64Decoder()],
    ["shareBps", getU64Decoder()],
    ["solPayoutAmount", getU64Decoder()],
    ["zincPayoutAmount", getU64Decoder()],
    ["basePaid", getBooleanDecoder()],
  ]);
}

export function getStockpileWinnerRankCodec(): FixedSizeCodec<
  StockpileWinnerRankArgs,
  StockpileWinnerRank
> {
  return combineCodec(
    getStockpileWinnerRankEncoder(),
    getStockpileWinnerRankDecoder(),
  );
}
