/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/** One stockpile player entry and its committed brick weight. */
export type StockpileEntry = {
  /** Joined player that owns this entry. */
  player: Address;
  /** Stockpile bricks committed by the player in `x10k` units. */
  bricksX10k: bigint;
};

export type StockpileEntryArgs = {
  /** Joined player that owns this entry. */
  player: Address;
  /** Stockpile bricks committed by the player in `x10k` units. */
  bricksX10k: number | bigint;
};

export function getStockpileEntryEncoder(): FixedSizeEncoder<StockpileEntryArgs> {
  return getStructEncoder([
    ["player", getAddressEncoder()],
    ["bricksX10k", getU64Encoder()],
  ]);
}

export function getStockpileEntryDecoder(): FixedSizeDecoder<StockpileEntry> {
  return getStructDecoder([
    ["player", getAddressDecoder()],
    ["bricksX10k", getU64Decoder()],
  ]);
}

export function getStockpileEntryCodec(): FixedSizeCodec<
  StockpileEntryArgs,
  StockpileEntry
> {
  return combineCodec(getStockpileEntryEncoder(), getStockpileEntryDecoder());
}
