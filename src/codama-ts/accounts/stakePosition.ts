/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const STAKE_POSITION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  78, 165, 30, 111, 171, 125, 11, 220,
]);

export function getStakePositionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    STAKE_POSITION_DISCRIMINATOR,
  );
}

export type StakePosition = {
  discriminator: ReadonlyUint8Array;
  /** Wallet that owns and signs for this position. */
  authority: Address;
  /** True once this position has been initialized for a specific authority. */
  initialized: boolean;
  /** PDA bump used to re-derive this position. */
  bump: number;
  /** Currently staked ZINC balance in mint base units. */
  balance: bigint;
  /** Treasury reward factor last seen by this position. */
  rewardsFactorCheckpoint: bigint;
  /** Claimable ZINC yield already checkpointed onto this position. */
  claimableRewards: bigint;
  /** Lifetime ZINC yield earned by this position. */
  lifetimeRewards: bigint;
};

export type StakePositionArgs = {
  /** Wallet that owns and signs for this position. */
  authority: Address;
  /** True once this position has been initialized for a specific authority. */
  initialized: boolean;
  /** PDA bump used to re-derive this position. */
  bump: number;
  /** Currently staked ZINC balance in mint base units. */
  balance: number | bigint;
  /** Treasury reward factor last seen by this position. */
  rewardsFactorCheckpoint: number | bigint;
  /** Claimable ZINC yield already checkpointed onto this position. */
  claimableRewards: number | bigint;
  /** Lifetime ZINC yield earned by this position. */
  lifetimeRewards: number | bigint;
};

/** Gets the encoder for {@link StakePositionArgs} account data. */
export function getStakePositionEncoder(): FixedSizeEncoder<StakePositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["initialized", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["balance", getU64Encoder()],
      ["rewardsFactorCheckpoint", getU128Encoder()],
      ["claimableRewards", getU64Encoder()],
      ["lifetimeRewards", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: STAKE_POSITION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link StakePosition} account data. */
export function getStakePositionDecoder(): FixedSizeDecoder<StakePosition> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["initialized", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["balance", getU64Decoder()],
    ["rewardsFactorCheckpoint", getU128Decoder()],
    ["claimableRewards", getU64Decoder()],
    ["lifetimeRewards", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link StakePosition} account data. */
export function getStakePositionCodec(): FixedSizeCodec<
  StakePositionArgs,
  StakePosition
> {
  return combineCodec(getStakePositionEncoder(), getStakePositionDecoder());
}

export function decodeStakePosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<StakePosition, TAddress>;
export function decodeStakePosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<StakePosition, TAddress>;
export function decodeStakePosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<StakePosition, TAddress> | MaybeAccount<StakePosition, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStakePositionDecoder(),
  );
}

export async function fetchStakePosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<StakePosition, TAddress>> {
  const maybeAccount = await fetchMaybeStakePosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStakePosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<StakePosition, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStakePosition(maybeAccount);
}

export async function fetchAllStakePosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<StakePosition>[]> {
  const maybeAccounts = await fetchAllMaybeStakePosition(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStakePosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<StakePosition>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeStakePosition(maybeAccount));
}

export function getStakePositionSize(): number {
  return 82;
}
