/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const STOCKPILE_SOL_VAULT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([116, 215, 19, 233, 185, 164, 92, 225]);

export function getStockpileSolVaultDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    STOCKPILE_SOL_VAULT_DISCRIMINATOR,
  );
}

export type StockpileSolVault = {
  discriminator: ReadonlyUint8Array;
  /** Whether the singleton has been initialized after account creation. */
  initialized: boolean;
  /** PDA bump used to re-derive the vault signer. */
  bump: number;
};

export type StockpileSolVaultArgs = {
  /** Whether the singleton has been initialized after account creation. */
  initialized: boolean;
  /** PDA bump used to re-derive the vault signer. */
  bump: number;
};

/** Gets the encoder for {@link StockpileSolVaultArgs} account data. */
export function getStockpileSolVaultEncoder(): FixedSizeEncoder<StockpileSolVaultArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["initialized", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: STOCKPILE_SOL_VAULT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link StockpileSolVault} account data. */
export function getStockpileSolVaultDecoder(): FixedSizeDecoder<StockpileSolVault> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["initialized", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link StockpileSolVault} account data. */
export function getStockpileSolVaultCodec(): FixedSizeCodec<
  StockpileSolVaultArgs,
  StockpileSolVault
> {
  return combineCodec(
    getStockpileSolVaultEncoder(),
    getStockpileSolVaultDecoder(),
  );
}

export function decodeStockpileSolVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<StockpileSolVault, TAddress>;
export function decodeStockpileSolVault<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<StockpileSolVault, TAddress>;
export function decodeStockpileSolVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<StockpileSolVault, TAddress>
  | MaybeAccount<StockpileSolVault, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStockpileSolVaultDecoder(),
  );
}

export async function fetchStockpileSolVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<StockpileSolVault, TAddress>> {
  const maybeAccount = await fetchMaybeStockpileSolVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStockpileSolVault<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<StockpileSolVault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStockpileSolVault(maybeAccount);
}

export async function fetchAllStockpileSolVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<StockpileSolVault>[]> {
  const maybeAccounts = await fetchAllMaybeStockpileSolVault(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStockpileSolVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<StockpileSolVault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeStockpileSolVault(maybeAccount),
  );
}

export function getStockpileSolVaultSize(): number {
  return 10;
}
