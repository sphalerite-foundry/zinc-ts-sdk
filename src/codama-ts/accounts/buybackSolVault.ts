/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BUYBACK_SOL_VAULT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([186, 89, 216, 116, 119, 22, 131, 194]);

export function getBuybackSolVaultDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUYBACK_SOL_VAULT_DISCRIMINATOR,
  );
}

export type BuybackSolVault = {
  discriminator: ReadonlyUint8Array;
  /** Whether the singleton has been initialized after account creation. */
  initialized: boolean;
  /** PDA bump used to re-derive the vault signer. */
  bump: number;
};

export type BuybackSolVaultArgs = {
  /** Whether the singleton has been initialized after account creation. */
  initialized: boolean;
  /** PDA bump used to re-derive the vault signer. */
  bump: number;
};

/** Gets the encoder for {@link BuybackSolVaultArgs} account data. */
export function getBuybackSolVaultEncoder(): FixedSizeEncoder<BuybackSolVaultArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["initialized", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BUYBACK_SOL_VAULT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link BuybackSolVault} account data. */
export function getBuybackSolVaultDecoder(): FixedSizeDecoder<BuybackSolVault> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["initialized", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link BuybackSolVault} account data. */
export function getBuybackSolVaultCodec(): FixedSizeCodec<
  BuybackSolVaultArgs,
  BuybackSolVault
> {
  return combineCodec(getBuybackSolVaultEncoder(), getBuybackSolVaultDecoder());
}

export function decodeBuybackSolVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<BuybackSolVault, TAddress>;
export function decodeBuybackSolVault<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<BuybackSolVault, TAddress>;
export function decodeBuybackSolVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<BuybackSolVault, TAddress>
  | MaybeAccount<BuybackSolVault, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBuybackSolVaultDecoder(),
  );
}

export async function fetchBuybackSolVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<BuybackSolVault, TAddress>> {
  const maybeAccount = await fetchMaybeBuybackSolVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBuybackSolVault<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<BuybackSolVault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBuybackSolVault(maybeAccount);
}

export async function fetchAllBuybackSolVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<BuybackSolVault>[]> {
  const maybeAccounts = await fetchAllMaybeBuybackSolVault(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBuybackSolVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<BuybackSolVault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeBuybackSolVault(maybeAccount),
  );
}

export function getBuybackSolVaultSize(): number {
  return 10;
}
