/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const AUTO_MINER_SESSION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([9, 91, 249, 138, 188, 84, 49, 61]);

export function getAutoMinerSessionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AUTO_MINER_SESSION_DISCRIMINATOR,
  );
}

export type AutoMinerSession = {
  discriminator: ReadonlyUint8Array;
  /** Wallet that owns and controls this session. */
  authority: Address;
  /** Crank wallet allowed to execute auto deploys. */
  executor: Address;
  /** Gross lamports to deploy into each eligible round. */
  amountPerRound: bigint;
  /** Ephemeral public key used to encrypt the tile-pattern bits to the crank. */
  maskBitsEncryptionKey: ReadonlyUint8Array;
  /** Rescue CTR nonce used for the encrypted tile-pattern bits. */
  maskBitsNonce: bigint;
  /** Encrypted tile-pattern bits readable only by the configured crank. */
  maskBitsCiphertext: ReadonlyUint8Array;
  /** Crank encryption-key version used by the browser when encrypting the pattern. */
  maskBitsKeyVersion: number;
  /** User-funded lamport budget available for deploys and reimbursements. */
  remainingBudget: bigint;
  /** Number of successful deploys already executed. */
  roundsUsed: bigint;
  /** Whether the authority paused auto execution. */
  paused: boolean;
  /** Fixed lamport reimbursement paid to the crank after a successful deploy. */
  crankReimbursementLamports: bigint;
  /** PDA bump seed. */
  bump: number;
  /** Optional last slot at which the session can still deploy. */
  expirySlot: Option<bigint>;
};

export type AutoMinerSessionArgs = {
  /** Wallet that owns and controls this session. */
  authority: Address;
  /** Crank wallet allowed to execute auto deploys. */
  executor: Address;
  /** Gross lamports to deploy into each eligible round. */
  amountPerRound: number | bigint;
  /** Ephemeral public key used to encrypt the tile-pattern bits to the crank. */
  maskBitsEncryptionKey: ReadonlyUint8Array;
  /** Rescue CTR nonce used for the encrypted tile-pattern bits. */
  maskBitsNonce: number | bigint;
  /** Encrypted tile-pattern bits readable only by the configured crank. */
  maskBitsCiphertext: ReadonlyUint8Array;
  /** Crank encryption-key version used by the browser when encrypting the pattern. */
  maskBitsKeyVersion: number;
  /** User-funded lamport budget available for deploys and reimbursements. */
  remainingBudget: number | bigint;
  /** Number of successful deploys already executed. */
  roundsUsed: number | bigint;
  /** Whether the authority paused auto execution. */
  paused: boolean;
  /** Fixed lamport reimbursement paid to the crank after a successful deploy. */
  crankReimbursementLamports: number | bigint;
  /** PDA bump seed. */
  bump: number;
  /** Optional last slot at which the session can still deploy. */
  expirySlot: OptionOrNullable<number | bigint>;
};

/** Gets the encoder for {@link AutoMinerSessionArgs} account data. */
export function getAutoMinerSessionEncoder(): Encoder<AutoMinerSessionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["executor", getAddressEncoder()],
      ["amountPerRound", getU64Encoder()],
      ["maskBitsEncryptionKey", fixEncoderSize(getBytesEncoder(), 32)],
      ["maskBitsNonce", getU128Encoder()],
      ["maskBitsCiphertext", fixEncoderSize(getBytesEncoder(), 32)],
      ["maskBitsKeyVersion", getU16Encoder()],
      ["remainingBudget", getU64Encoder()],
      ["roundsUsed", getU64Encoder()],
      ["paused", getBooleanEncoder()],
      ["crankReimbursementLamports", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["expirySlot", getOptionEncoder(getU64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: AUTO_MINER_SESSION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AutoMinerSession} account data. */
export function getAutoMinerSessionDecoder(): Decoder<AutoMinerSession> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["executor", getAddressDecoder()],
    ["amountPerRound", getU64Decoder()],
    ["maskBitsEncryptionKey", fixDecoderSize(getBytesDecoder(), 32)],
    ["maskBitsNonce", getU128Decoder()],
    ["maskBitsCiphertext", fixDecoderSize(getBytesDecoder(), 32)],
    ["maskBitsKeyVersion", getU16Decoder()],
    ["remainingBudget", getU64Decoder()],
    ["roundsUsed", getU64Decoder()],
    ["paused", getBooleanDecoder()],
    ["crankReimbursementLamports", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["expirySlot", getOptionDecoder(getU64Decoder())],
  ]);
}

/** Gets the codec for {@link AutoMinerSession} account data. */
export function getAutoMinerSessionCodec(): Codec<
  AutoMinerSessionArgs,
  AutoMinerSession
> {
  return combineCodec(
    getAutoMinerSessionEncoder(),
    getAutoMinerSessionDecoder(),
  );
}

export function decodeAutoMinerSession<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AutoMinerSession, TAddress>;
export function decodeAutoMinerSession<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AutoMinerSession, TAddress>;
export function decodeAutoMinerSession<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<AutoMinerSession, TAddress>
  | MaybeAccount<AutoMinerSession, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAutoMinerSessionDecoder(),
  );
}

export async function fetchAutoMinerSession<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AutoMinerSession, TAddress>> {
  const maybeAccount = await fetchMaybeAutoMinerSession(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAutoMinerSession<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AutoMinerSession, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAutoMinerSession(maybeAccount);
}

export async function fetchAllAutoMinerSession(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AutoMinerSession>[]> {
  const maybeAccounts = await fetchAllMaybeAutoMinerSession(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAutoMinerSession(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AutoMinerSession>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeAutoMinerSession(maybeAccount),
  );
}
