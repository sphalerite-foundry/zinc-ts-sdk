/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BUYBACK_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  30, 9, 48, 171, 48, 116, 22, 165,
]);

export function getBuybackPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUYBACK_POOL_DISCRIMINATOR,
  );
}

export type BuybackPool = {
  discriminator: ReadonlyUint8Array;
  /** PDA bump used to re-derive this account. */
  bump: number;
  /** Meteora DAMM v2 program that owns the pool. */
  meteoraProgram: Address;
  /** Meteora static pool config used to initialize the pool. */
  dammConfig: Address;
  /** Meteora pool authority PDA. */
  poolAuthority: Address;
  /** Meteora pool account used for buybacks. */
  pool: Address;
  /** Initial liquidity position account for the pool. */
  position: Address;
  /** Token-2022 NFT mint representing the initial position. */
  positionNftMint: Address;
  /** Token-2022 account holding the initial position NFT. */
  positionNftAccount: Address;
  /** Pool token A mint. */
  tokenAMint: Address;
  /** Pool token B mint. */
  tokenBMint: Address;
  /** Pool token A vault. */
  tokenAVault: Address;
  /** Pool token B vault. */
  tokenBVault: Address;
  /** SPL token program for token A. */
  tokenAProgram: Address;
  /** SPL token program for token B. */
  tokenBProgram: Address;
  /** Meteora event authority PDA required by event CPI instructions. */
  eventAuthority: Address;
  /** Whether the pool's token A side is WSOL. */
  wsolIsTokenA: boolean;
  /** Initial WSOL amount deposited into the pool. */
  initialWsolLamports: bigint;
  /** Initial ZINC amount deposited into the pool. */
  initialZincAmount: bigint;
  /** Initial Meteora liquidity parameter used during pool creation. */
  liquidity: bigint;
  /** Initial sqrt price parameter used during pool creation. */
  sqrtPrice: bigint;
  /** Optional activation point used during pool creation. */
  activationPoint: Option<bigint>;
};

export type BuybackPoolArgs = {
  /** PDA bump used to re-derive this account. */
  bump: number;
  /** Meteora DAMM v2 program that owns the pool. */
  meteoraProgram: Address;
  /** Meteora static pool config used to initialize the pool. */
  dammConfig: Address;
  /** Meteora pool authority PDA. */
  poolAuthority: Address;
  /** Meteora pool account used for buybacks. */
  pool: Address;
  /** Initial liquidity position account for the pool. */
  position: Address;
  /** Token-2022 NFT mint representing the initial position. */
  positionNftMint: Address;
  /** Token-2022 account holding the initial position NFT. */
  positionNftAccount: Address;
  /** Pool token A mint. */
  tokenAMint: Address;
  /** Pool token B mint. */
  tokenBMint: Address;
  /** Pool token A vault. */
  tokenAVault: Address;
  /** Pool token B vault. */
  tokenBVault: Address;
  /** SPL token program for token A. */
  tokenAProgram: Address;
  /** SPL token program for token B. */
  tokenBProgram: Address;
  /** Meteora event authority PDA required by event CPI instructions. */
  eventAuthority: Address;
  /** Whether the pool's token A side is WSOL. */
  wsolIsTokenA: boolean;
  /** Initial WSOL amount deposited into the pool. */
  initialWsolLamports: number | bigint;
  /** Initial ZINC amount deposited into the pool. */
  initialZincAmount: number | bigint;
  /** Initial Meteora liquidity parameter used during pool creation. */
  liquidity: number | bigint;
  /** Initial sqrt price parameter used during pool creation. */
  sqrtPrice: number | bigint;
  /** Optional activation point used during pool creation. */
  activationPoint: OptionOrNullable<number | bigint>;
};

/** Gets the encoder for {@link BuybackPoolArgs} account data. */
export function getBuybackPoolEncoder(): Encoder<BuybackPoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["meteoraProgram", getAddressEncoder()],
      ["dammConfig", getAddressEncoder()],
      ["poolAuthority", getAddressEncoder()],
      ["pool", getAddressEncoder()],
      ["position", getAddressEncoder()],
      ["positionNftMint", getAddressEncoder()],
      ["positionNftAccount", getAddressEncoder()],
      ["tokenAMint", getAddressEncoder()],
      ["tokenBMint", getAddressEncoder()],
      ["tokenAVault", getAddressEncoder()],
      ["tokenBVault", getAddressEncoder()],
      ["tokenAProgram", getAddressEncoder()],
      ["tokenBProgram", getAddressEncoder()],
      ["eventAuthority", getAddressEncoder()],
      ["wsolIsTokenA", getBooleanEncoder()],
      ["initialWsolLamports", getU64Encoder()],
      ["initialZincAmount", getU64Encoder()],
      ["liquidity", getU128Encoder()],
      ["sqrtPrice", getU128Encoder()],
      ["activationPoint", getOptionEncoder(getU64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: BUYBACK_POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link BuybackPool} account data. */
export function getBuybackPoolDecoder(): Decoder<BuybackPool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["meteoraProgram", getAddressDecoder()],
    ["dammConfig", getAddressDecoder()],
    ["poolAuthority", getAddressDecoder()],
    ["pool", getAddressDecoder()],
    ["position", getAddressDecoder()],
    ["positionNftMint", getAddressDecoder()],
    ["positionNftAccount", getAddressDecoder()],
    ["tokenAMint", getAddressDecoder()],
    ["tokenBMint", getAddressDecoder()],
    ["tokenAVault", getAddressDecoder()],
    ["tokenBVault", getAddressDecoder()],
    ["tokenAProgram", getAddressDecoder()],
    ["tokenBProgram", getAddressDecoder()],
    ["eventAuthority", getAddressDecoder()],
    ["wsolIsTokenA", getBooleanDecoder()],
    ["initialWsolLamports", getU64Decoder()],
    ["initialZincAmount", getU64Decoder()],
    ["liquidity", getU128Decoder()],
    ["sqrtPrice", getU128Decoder()],
    ["activationPoint", getOptionDecoder(getU64Decoder())],
  ]);
}

/** Gets the codec for {@link BuybackPool} account data. */
export function getBuybackPoolCodec(): Codec<BuybackPoolArgs, BuybackPool> {
  return combineCodec(getBuybackPoolEncoder(), getBuybackPoolDecoder());
}

export function decodeBuybackPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<BuybackPool, TAddress>;
export function decodeBuybackPool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<BuybackPool, TAddress>;
export function decodeBuybackPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<BuybackPool, TAddress> | MaybeAccount<BuybackPool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBuybackPoolDecoder(),
  );
}

export async function fetchBuybackPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<BuybackPool, TAddress>> {
  const maybeAccount = await fetchMaybeBuybackPool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBuybackPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<BuybackPool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBuybackPool(maybeAccount);
}

export async function fetchAllBuybackPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<BuybackPool>[]> {
  const maybeAccounts = await fetchAllMaybeBuybackPool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBuybackPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<BuybackPool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBuybackPool(maybeAccount));
}
