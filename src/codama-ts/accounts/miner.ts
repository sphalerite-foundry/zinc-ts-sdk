/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const MINER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  223, 113, 15, 54, 123, 122, 140, 100,
]);

export function getMinerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MINER_DISCRIMINATOR);
}

export type Miner = {
  discriminator: ReadonlyUint8Array;
  /** Sequential round identifier. */
  roundId: bigint;
  /** Owner. */
  player: Address;
  /** Total net amount staked for this round across all selected tiles after deploy-time fees. */
  totalAmount: bigint;
  /** X25519 public key used when encrypting the mask. */
  maskEncryptionKey: ReadonlyUint8Array;
  /** Rescue CTR nonce for the encrypted mask. */
  maskNonce: bigint;
  /** Ciphertext limbs for `Enc<Shared, Pack<[u8; 30]>>`. */
  maskCiphertext: ReadonlyUint8Array;
  /** Whether this miner's SOL payout has been claimed. */
  solClaimed: boolean;
  /** Whether this miner's ZINC reward has been credited to the player's profile. */
  zincCredited: boolean;
  /** PDA bump seed. */
  bump: number;
  /**
   * Amount effectively placed on the winning tile after settlement.
   * `None` means this miner has not been processed by batch settlement yet.
   */
  winningStake: Option<bigint>;
  /** Stable zero-based deployment order within the round. */
  minerIndex: bigint;
  /** Hidden bonus stockpile bricks awarded during settlement, in `x10k` units. */
  hiddenBonusBricksX10k: bigint;
  /** Auto-miner session PDA that created this miner, or `None` for manual and legacy miners. */
  autoMinerSession: Option<Address>;
};

export type MinerArgs = {
  /** Sequential round identifier. */
  roundId: number | bigint;
  /** Owner. */
  player: Address;
  /** Total net amount staked for this round across all selected tiles after deploy-time fees. */
  totalAmount: number | bigint;
  /** X25519 public key used when encrypting the mask. */
  maskEncryptionKey: ReadonlyUint8Array;
  /** Rescue CTR nonce for the encrypted mask. */
  maskNonce: number | bigint;
  /** Ciphertext limbs for `Enc<Shared, Pack<[u8; 30]>>`. */
  maskCiphertext: ReadonlyUint8Array;
  /** Whether this miner's SOL payout has been claimed. */
  solClaimed: boolean;
  /** Whether this miner's ZINC reward has been credited to the player's profile. */
  zincCredited: boolean;
  /** PDA bump seed. */
  bump: number;
  /**
   * Amount effectively placed on the winning tile after settlement.
   * `None` means this miner has not been processed by batch settlement yet.
   */
  winningStake: OptionOrNullable<number | bigint>;
  /** Stable zero-based deployment order within the round. */
  minerIndex: number | bigint;
  /** Hidden bonus stockpile bricks awarded during settlement, in `x10k` units. */
  hiddenBonusBricksX10k: number | bigint;
  /** Auto-miner session PDA that created this miner, or `None` for manual and legacy miners. */
  autoMinerSession: OptionOrNullable<Address>;
};

/** Gets the encoder for {@link MinerArgs} account data. */
export function getMinerEncoder(): Encoder<MinerArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU64Encoder()],
      ["player", getAddressEncoder()],
      ["totalAmount", getU64Encoder()],
      ["maskEncryptionKey", fixEncoderSize(getBytesEncoder(), 32)],
      ["maskNonce", getU128Encoder()],
      ["maskCiphertext", fixEncoderSize(getBytesEncoder(), 64)],
      ["solClaimed", getBooleanEncoder()],
      ["zincCredited", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["winningStake", getOptionEncoder(getU64Encoder())],
      ["minerIndex", getU64Encoder()],
      ["hiddenBonusBricksX10k", getU64Encoder()],
      ["autoMinerSession", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: MINER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Miner} account data. */
export function getMinerDecoder(): Decoder<Miner> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU64Decoder()],
    ["player", getAddressDecoder()],
    ["totalAmount", getU64Decoder()],
    ["maskEncryptionKey", fixDecoderSize(getBytesDecoder(), 32)],
    ["maskNonce", getU128Decoder()],
    ["maskCiphertext", fixDecoderSize(getBytesDecoder(), 64)],
    ["solClaimed", getBooleanDecoder()],
    ["zincCredited", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["winningStake", getOptionDecoder(getU64Decoder())],
    ["minerIndex", getU64Decoder()],
    ["hiddenBonusBricksX10k", getU64Decoder()],
    ["autoMinerSession", getOptionDecoder(getAddressDecoder())],
  ]);
}

/** Gets the codec for {@link Miner} account data. */
export function getMinerCodec(): Codec<MinerArgs, Miner> {
  return combineCodec(getMinerEncoder(), getMinerDecoder());
}

export function decodeMiner<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Miner, TAddress>;
export function decodeMiner<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Miner, TAddress>;
export function decodeMiner<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Miner, TAddress> | MaybeAccount<Miner, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getMinerDecoder(),
  );
}

export async function fetchMiner<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Miner, TAddress>> {
  const maybeAccount = await fetchMaybeMiner(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMiner<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Miner, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMiner(maybeAccount);
}

export async function fetchAllMiner(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Miner>[]> {
  const maybeAccounts = await fetchAllMaybeMiner(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMiner(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Miner>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMiner(maybeAccount));
}
