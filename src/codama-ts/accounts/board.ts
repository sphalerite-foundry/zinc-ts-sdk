/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BOARD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  79, 48, 160, 63, 153, 132, 240, 56,
]);

export function getBoardDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BOARD_DISCRIMINATOR);
}

export type Board = {
  discriminator: ReadonlyUint8Array;
  /** ID of the round currently enabled for deposits, if any. */
  activeRoundId: Option<bigint>;
  /** ID of the stockpile currently open for joins, if any. */
  activeStockpileId: Option<bigint>;
  /** ID of the stockpile that still has unresolved lifecycle work, if any. */
  unresolvedStockpileId: Option<bigint>;
  /** ID of the round waiting for its first deploy, if any. */
  waitingRoundId: Option<bigint>;
  /** Next sequential round ID to allocate when preparing future rounds. */
  nextRoundId: bigint;
  /** Next sequential stockpile ID to allocate when preparing future cycles. */
  nextStockpileId: bigint;
  /** Authority allowed to rotate and settle rounds. */
  crankAuthority: Address;
  /** PDA bump seed. */
  bump: number;
};

export type BoardArgs = {
  /** ID of the round currently enabled for deposits, if any. */
  activeRoundId: OptionOrNullable<number | bigint>;
  /** ID of the stockpile currently open for joins, if any. */
  activeStockpileId: OptionOrNullable<number | bigint>;
  /** ID of the stockpile that still has unresolved lifecycle work, if any. */
  unresolvedStockpileId: OptionOrNullable<number | bigint>;
  /** ID of the round waiting for its first deploy, if any. */
  waitingRoundId: OptionOrNullable<number | bigint>;
  /** Next sequential round ID to allocate when preparing future rounds. */
  nextRoundId: number | bigint;
  /** Next sequential stockpile ID to allocate when preparing future cycles. */
  nextStockpileId: number | bigint;
  /** Authority allowed to rotate and settle rounds. */
  crankAuthority: Address;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link BoardArgs} account data. */
export function getBoardEncoder(): Encoder<BoardArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["activeRoundId", getOptionEncoder(getU64Encoder())],
      ["activeStockpileId", getOptionEncoder(getU64Encoder())],
      ["unresolvedStockpileId", getOptionEncoder(getU64Encoder())],
      ["waitingRoundId", getOptionEncoder(getU64Encoder())],
      ["nextRoundId", getU64Encoder()],
      ["nextStockpileId", getU64Encoder()],
      ["crankAuthority", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BOARD_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Board} account data. */
export function getBoardDecoder(): Decoder<Board> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["activeRoundId", getOptionDecoder(getU64Decoder())],
    ["activeStockpileId", getOptionDecoder(getU64Decoder())],
    ["unresolvedStockpileId", getOptionDecoder(getU64Decoder())],
    ["waitingRoundId", getOptionDecoder(getU64Decoder())],
    ["nextRoundId", getU64Decoder()],
    ["nextStockpileId", getU64Decoder()],
    ["crankAuthority", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Board} account data. */
export function getBoardCodec(): Codec<BoardArgs, Board> {
  return combineCodec(getBoardEncoder(), getBoardDecoder());
}

export function decodeBoard<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Board, TAddress>;
export function decodeBoard<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Board, TAddress>;
export function decodeBoard<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Board, TAddress> | MaybeAccount<Board, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBoardDecoder()
  );
}

export async function fetchBoard<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Board, TAddress>> {
  const maybeAccount = await fetchMaybeBoard(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBoard<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Board, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBoard(maybeAccount);
}

export async function fetchAllBoard(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Board>[]> {
  const maybeAccounts = await fetchAllMaybeBoard(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBoard(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Board>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBoard(maybeAccount));
}
