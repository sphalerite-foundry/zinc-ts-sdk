/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getStockpileEntryRangeDecoder,
  getStockpileEntryRangeEncoder,
  getStockpileStatusDecoder,
  getStockpileStatusEncoder,
  type StockpileEntryRange,
  type StockpileEntryRangeArgs,
  type StockpileStatus,
  type StockpileStatusArgs,
} from "../types";

export const STOCKPILE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  218, 41, 100, 84, 84, 16, 69, 252,
]);

export function getStockpileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(STOCKPILE_DISCRIMINATOR);
}

export type Stockpile = {
  discriminator: ReadonlyUint8Array;
  /** Lifecycle state for this stockpile cycle. */
  status: StockpileStatus;
  /** Sequential stockpile identifier. */
  id: bigint;
  /** True once this PDA has been initialized for a concrete stockpile cycle. */
  initialized: boolean;
  /** Configured slot duration captured when the cycle was created. */
  durationSlots: bigint;
  /** Slot when the cycle opened for joins. */
  openSlot: Option<bigint>;
  /** Slot after which the cycle may be closed. */
  closeAfterSlot: Option<bigint>;
  /** Number of joined entries in this cycle. */
  entrantCount: bigint;
  /** Total committed stockpile bricks in `x10k` units. */
  totalCommittedBricksX10k: bigint;
  /** Revealed random value, if any. */
  rand: Option<bigint>;
  /** Winning ticket index in `x10k` units, if any. */
  winningTicketX10k: Option<bigint>;
  /** Winner selected for payout, if any. */
  winner: Option<Address>;
  /** Snapshotted SOL payout reserved for this cycle. */
  solPayoutAmount: bigint;
  /** Snapshotted ZINC payout reserved for this cycle. */
  zincPayoutAmount: bigint;
  /** PDA bump seed. */
  bump: number;
  /** All joined ranges for this cycle. */
  entries: Array<StockpileEntryRange>;
};

export type StockpileArgs = {
  /** Lifecycle state for this stockpile cycle. */
  status: StockpileStatusArgs;
  /** Sequential stockpile identifier. */
  id: number | bigint;
  /** True once this PDA has been initialized for a concrete stockpile cycle. */
  initialized: boolean;
  /** Configured slot duration captured when the cycle was created. */
  durationSlots: number | bigint;
  /** Slot when the cycle opened for joins. */
  openSlot: OptionOrNullable<number | bigint>;
  /** Slot after which the cycle may be closed. */
  closeAfterSlot: OptionOrNullable<number | bigint>;
  /** Number of joined entries in this cycle. */
  entrantCount: number | bigint;
  /** Total committed stockpile bricks in `x10k` units. */
  totalCommittedBricksX10k: number | bigint;
  /** Revealed random value, if any. */
  rand: OptionOrNullable<number | bigint>;
  /** Winning ticket index in `x10k` units, if any. */
  winningTicketX10k: OptionOrNullable<number | bigint>;
  /** Winner selected for payout, if any. */
  winner: OptionOrNullable<Address>;
  /** Snapshotted SOL payout reserved for this cycle. */
  solPayoutAmount: number | bigint;
  /** Snapshotted ZINC payout reserved for this cycle. */
  zincPayoutAmount: number | bigint;
  /** PDA bump seed. */
  bump: number;
  /** All joined ranges for this cycle. */
  entries: Array<StockpileEntryRangeArgs>;
};

/** Gets the encoder for {@link StockpileArgs} account data. */
export function getStockpileEncoder(): Encoder<StockpileArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["status", getStockpileStatusEncoder()],
      ["id", getU64Encoder()],
      ["initialized", getBooleanEncoder()],
      ["durationSlots", getU64Encoder()],
      ["openSlot", getOptionEncoder(getU64Encoder())],
      ["closeAfterSlot", getOptionEncoder(getU64Encoder())],
      ["entrantCount", getU64Encoder()],
      ["totalCommittedBricksX10k", getU64Encoder()],
      ["rand", getOptionEncoder(getU64Encoder())],
      ["winningTicketX10k", getOptionEncoder(getU64Encoder())],
      ["winner", getOptionEncoder(getAddressEncoder())],
      ["solPayoutAmount", getU64Encoder()],
      ["zincPayoutAmount", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["entries", getArrayEncoder(getStockpileEntryRangeEncoder())],
    ]),
    (value) => ({ ...value, discriminator: STOCKPILE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Stockpile} account data. */
export function getStockpileDecoder(): Decoder<Stockpile> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["status", getStockpileStatusDecoder()],
    ["id", getU64Decoder()],
    ["initialized", getBooleanDecoder()],
    ["durationSlots", getU64Decoder()],
    ["openSlot", getOptionDecoder(getU64Decoder())],
    ["closeAfterSlot", getOptionDecoder(getU64Decoder())],
    ["entrantCount", getU64Decoder()],
    ["totalCommittedBricksX10k", getU64Decoder()],
    ["rand", getOptionDecoder(getU64Decoder())],
    ["winningTicketX10k", getOptionDecoder(getU64Decoder())],
    ["winner", getOptionDecoder(getAddressDecoder())],
    ["solPayoutAmount", getU64Decoder()],
    ["zincPayoutAmount", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["entries", getArrayDecoder(getStockpileEntryRangeDecoder())],
  ]);
}

/** Gets the codec for {@link Stockpile} account data. */
export function getStockpileCodec(): Codec<StockpileArgs, Stockpile> {
  return combineCodec(getStockpileEncoder(), getStockpileDecoder());
}

export function decodeStockpile<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Stockpile, TAddress>;
export function decodeStockpile<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Stockpile, TAddress>;
export function decodeStockpile<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Stockpile, TAddress> | MaybeAccount<Stockpile, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStockpileDecoder(),
  );
}

export async function fetchStockpile<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Stockpile, TAddress>> {
  const maybeAccount = await fetchMaybeStockpile(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStockpile<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Stockpile, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStockpile(maybeAccount);
}

export async function fetchAllStockpile(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Stockpile>[]> {
  const maybeAccounts = await fetchAllMaybeStockpile(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStockpile(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Stockpile>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeStockpile(maybeAccount));
}
