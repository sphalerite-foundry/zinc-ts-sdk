/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const TREASURY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  238, 239, 123, 238, 89, 1, 168, 253,
]);

export function getTreasuryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TREASURY_DISCRIMINATOR);
}

export type Treasury = {
  discriminator: ReadonlyUint8Array;
  /** PDA bump used to re-derive the treasury signer. */
  bump: number;
  /** Protocol ZINC mint created during `init_config`. */
  zincMint: Address;
  /** Curve-admin token account created during `init_config`. */
  curveAdminTokenAccount: Address;
  /** Bonanza token account created during `init_config`. */
  bonanzaTokenAccount: Address;
  /** Stockpile token account created during `init_config`. */
  stockpileTokenAccount: Address;
  /** Stockpile SOL vault created during `init_config`. */
  stockpileSolVault: Address;
  /** Buyback SOL vault created during `init_config`. */
  buybackSolVault: Address;
  /** Lifetime ZINC minted into treasury-controlled round and stockpile inventory. */
  totalZincMinted: bigint;
  /** Lifetime ZINC routed through the shared melt sink from direct melts and buybacks. */
  totalZincMelted: bigint;
  /** Current undistributed Bonanza pot held in the dedicated Bonanza vault. */
  bonanzaPot: bigint;
  /** Total ZINC currently staked into the protocol staking vault. */
  totalStaked: bigint;
  /** Cumulative staking reward factor shared by all stake positions. */
  stakingRewardsFactor: bigint;
  /** Pool-level carry remainder used when converting one reward amount into the cumulative factor. */
  stakingRewardsRemainder: bigint;
};

export type TreasuryArgs = {
  /** PDA bump used to re-derive the treasury signer. */
  bump: number;
  /** Protocol ZINC mint created during `init_config`. */
  zincMint: Address;
  /** Curve-admin token account created during `init_config`. */
  curveAdminTokenAccount: Address;
  /** Bonanza token account created during `init_config`. */
  bonanzaTokenAccount: Address;
  /** Stockpile token account created during `init_config`. */
  stockpileTokenAccount: Address;
  /** Stockpile SOL vault created during `init_config`. */
  stockpileSolVault: Address;
  /** Buyback SOL vault created during `init_config`. */
  buybackSolVault: Address;
  /** Lifetime ZINC minted into treasury-controlled round and stockpile inventory. */
  totalZincMinted: number | bigint;
  /** Lifetime ZINC routed through the shared melt sink from direct melts and buybacks. */
  totalZincMelted: number | bigint;
  /** Current undistributed Bonanza pot held in the dedicated Bonanza vault. */
  bonanzaPot: number | bigint;
  /** Total ZINC currently staked into the protocol staking vault. */
  totalStaked: number | bigint;
  /** Cumulative staking reward factor shared by all stake positions. */
  stakingRewardsFactor: number | bigint;
  /** Pool-level carry remainder used when converting one reward amount into the cumulative factor. */
  stakingRewardsRemainder: number | bigint;
};

/** Gets the encoder for {@link TreasuryArgs} account data. */
export function getTreasuryEncoder(): FixedSizeEncoder<TreasuryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["zincMint", getAddressEncoder()],
      ["curveAdminTokenAccount", getAddressEncoder()],
      ["bonanzaTokenAccount", getAddressEncoder()],
      ["stockpileTokenAccount", getAddressEncoder()],
      ["stockpileSolVault", getAddressEncoder()],
      ["buybackSolVault", getAddressEncoder()],
      ["totalZincMinted", getU64Encoder()],
      ["totalZincMelted", getU64Encoder()],
      ["bonanzaPot", getU64Encoder()],
      ["totalStaked", getU64Encoder()],
      ["stakingRewardsFactor", getU128Encoder()],
      ["stakingRewardsRemainder", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TREASURY_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Treasury} account data. */
export function getTreasuryDecoder(): FixedSizeDecoder<Treasury> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["zincMint", getAddressDecoder()],
    ["curveAdminTokenAccount", getAddressDecoder()],
    ["bonanzaTokenAccount", getAddressDecoder()],
    ["stockpileTokenAccount", getAddressDecoder()],
    ["stockpileSolVault", getAddressDecoder()],
    ["buybackSolVault", getAddressDecoder()],
    ["totalZincMinted", getU64Decoder()],
    ["totalZincMelted", getU64Decoder()],
    ["bonanzaPot", getU64Decoder()],
    ["totalStaked", getU64Decoder()],
    ["stakingRewardsFactor", getU128Decoder()],
    ["stakingRewardsRemainder", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link Treasury} account data. */
export function getTreasuryCodec(): FixedSizeCodec<TreasuryArgs, Treasury> {
  return combineCodec(getTreasuryEncoder(), getTreasuryDecoder());
}

export function decodeTreasury<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Treasury, TAddress>;
export function decodeTreasury<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Treasury, TAddress>;
export function decodeTreasury<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Treasury, TAddress> | MaybeAccount<Treasury, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTreasuryDecoder()
  );
}

export async function fetchTreasury<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Treasury, TAddress>> {
  const maybeAccount = await fetchMaybeTreasury(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTreasury<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Treasury, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTreasury(maybeAccount);
}

export async function fetchAllTreasury(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Treasury>[]> {
  const maybeAccounts = await fetchAllMaybeTreasury(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTreasury(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Treasury>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTreasury(maybeAccount));
}

export function getTreasurySize(): number {
  return 257;
}
