/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getRoundStatusDecoder,
  getRoundStatusEncoder,
  getRoundWildcatEntryRangeDecoder,
  getRoundWildcatEntryRangeEncoder,
  type RoundStatus,
  type RoundStatusArgs,
  type RoundWildcatEntryRange,
  type RoundWildcatEntryRangeArgs,
} from "../types";

export const ROUND_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  87, 127, 165, 51, 73, 78, 116, 174,
]);

export function getRoundDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ROUND_DISCRIMINATOR);
}

export type Round = {
  discriminator: ReadonlyUint8Array;
  /** Lifecycle state for preparation, play, and settlement. */
  status: RoundStatus;
  /** Sequential round identifier. */
  id: bigint;
  /** True once this PDA has been initialized for a specific round ID. */
  initialized: boolean;
  /** Slot when the round became active. */
  startSlot: Option<bigint>;
  /** Slot when the round stopped accepting deposits. */
  endSlot: Option<bigint>;
  /** Slot at which the active round stops accepting new deposits. */
  closeAfterSlot: Option<bigint>;
  /** Total net lamports in play across all miners in the round after deploy-time fees. */
  totalDeployed: bigint;
  /** Total buyback lamports generated by deploys in this round. */
  buybackFeeLamports: bigint;
  /** Public winning square after randomness reveal. */
  winningSquare: Option<bigint>;
  /** Aggregate net amount effectively placed on the winning tile. */
  totalOnWinningTile: bigint;
  /** Number of winners in the round. */
  winners: bigint;
  /** Remaining winners whose ZINC rewards still need to be credited. */
  remainingWinnerClaims: bigint;
  /** Total number of players in the round. */
  numberOfPlayers: bigint;
  /** Number of players already processed by batch settlement. */
  settledMinerCount: bigint;
  /** Number of queued settlement batch callbacks that have not landed yet. */
  pendingSettlementBatchCallbacks: bigint;
  /** Public random value revealed during settlement, absent beforehand. */
  rand: Option<bigint>;
  /** ZINC Bonanza payout reserved for this round's winners, if the stockpile resolved here. */
  bonanzaPayoutAmount: bigint;
  /** PDA bump seed. */
  bump: number;
  /** Direct-winner curve ZINC payout reserved for this round's winners at round close. */
  directWinnerZincPayoutAmount: bigint;
  /** Number of miner accounts already closed during post-terminal cleanup. */
  closedMinerCount: bigint;
  /** Direct-winner ZINC amount reserved for Wildcat, or zero when inactive. */
  wildcatZincPayoutAmount: bigint;
  /** True when the revealed randomness selected this round for Wildcat. */
  isWildcatRound: boolean;
  /** Total hidden-bonus-brick weight from settled round winners eligible for Wildcat. */
  totalWildcatWeight: bigint;
  /** Deterministic weighted ticket selected from `round.rand`, absent until selection starts. */
  wildcatTicket: Option<bigint>;
  /** Selected winning miner PDA for Wildcat. */
  wildcatWinnerMiner: Option<Address>;
  /** Selected winning player wallet for Wildcat. */
  wildcatWinnerPlayer: Option<Address>;
  /** True once the selection crank has fully resolved this round's Wildcat draw. */
  wildcatSelectionComplete: boolean;
  /** True once the selected Wildcat payout has been credited or the draw needed no transfer. */
  wildcatClaimed: boolean;
  /** Accumulator mixed from every deploy's public and encrypted trace. */
  blockhashEntropyAccumulator: ReadonlyUint8Array;
  /** Number of deploy entropy contributions mixed into the accumulator. */
  blockhashEntropyContributionCount: bigint;
  /** Weighted ticket ranges for settled winners eligible for Wildcat. */
  wildcatEntries: Array<RoundWildcatEntryRange>;
};

export type RoundArgs = {
  /** Lifecycle state for preparation, play, and settlement. */
  status: RoundStatusArgs;
  /** Sequential round identifier. */
  id: number | bigint;
  /** True once this PDA has been initialized for a specific round ID. */
  initialized: boolean;
  /** Slot when the round became active. */
  startSlot: OptionOrNullable<number | bigint>;
  /** Slot when the round stopped accepting deposits. */
  endSlot: OptionOrNullable<number | bigint>;
  /** Slot at which the active round stops accepting new deposits. */
  closeAfterSlot: OptionOrNullable<number | bigint>;
  /** Total net lamports in play across all miners in the round after deploy-time fees. */
  totalDeployed: number | bigint;
  /** Total buyback lamports generated by deploys in this round. */
  buybackFeeLamports: number | bigint;
  /** Public winning square after randomness reveal. */
  winningSquare: OptionOrNullable<number | bigint>;
  /** Aggregate net amount effectively placed on the winning tile. */
  totalOnWinningTile: number | bigint;
  /** Number of winners in the round. */
  winners: number | bigint;
  /** Remaining winners whose ZINC rewards still need to be credited. */
  remainingWinnerClaims: number | bigint;
  /** Total number of players in the round. */
  numberOfPlayers: number | bigint;
  /** Number of players already processed by batch settlement. */
  settledMinerCount: number | bigint;
  /** Number of queued settlement batch callbacks that have not landed yet. */
  pendingSettlementBatchCallbacks: number | bigint;
  /** Public random value revealed during settlement, absent beforehand. */
  rand: OptionOrNullable<number | bigint>;
  /** ZINC Bonanza payout reserved for this round's winners, if the stockpile resolved here. */
  bonanzaPayoutAmount: number | bigint;
  /** PDA bump seed. */
  bump: number;
  /** Direct-winner curve ZINC payout reserved for this round's winners at round close. */
  directWinnerZincPayoutAmount: number | bigint;
  /** Number of miner accounts already closed during post-terminal cleanup. */
  closedMinerCount: number | bigint;
  /** Direct-winner ZINC amount reserved for Wildcat, or zero when inactive. */
  wildcatZincPayoutAmount: number | bigint;
  /** True when the revealed randomness selected this round for Wildcat. */
  isWildcatRound: boolean;
  /** Total hidden-bonus-brick weight from settled round winners eligible for Wildcat. */
  totalWildcatWeight: number | bigint;
  /** Deterministic weighted ticket selected from `round.rand`, absent until selection starts. */
  wildcatTicket: OptionOrNullable<number | bigint>;
  /** Selected winning miner PDA for Wildcat. */
  wildcatWinnerMiner: OptionOrNullable<Address>;
  /** Selected winning player wallet for Wildcat. */
  wildcatWinnerPlayer: OptionOrNullable<Address>;
  /** True once the selection crank has fully resolved this round's Wildcat draw. */
  wildcatSelectionComplete: boolean;
  /** True once the selected Wildcat payout has been credited or the draw needed no transfer. */
  wildcatClaimed: boolean;
  /** Accumulator mixed from every deploy's public and encrypted trace. */
  blockhashEntropyAccumulator: ReadonlyUint8Array;
  /** Number of deploy entropy contributions mixed into the accumulator. */
  blockhashEntropyContributionCount: number | bigint;
  /** Weighted ticket ranges for settled winners eligible for Wildcat. */
  wildcatEntries: Array<RoundWildcatEntryRangeArgs>;
};

/** Gets the encoder for {@link RoundArgs} account data. */
export function getRoundEncoder(): Encoder<RoundArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["status", getRoundStatusEncoder()],
      ["id", getU64Encoder()],
      ["initialized", getBooleanEncoder()],
      ["startSlot", getOptionEncoder(getU64Encoder())],
      ["endSlot", getOptionEncoder(getU64Encoder())],
      ["closeAfterSlot", getOptionEncoder(getU64Encoder())],
      ["totalDeployed", getU64Encoder()],
      ["buybackFeeLamports", getU64Encoder()],
      ["winningSquare", getOptionEncoder(getU64Encoder())],
      ["totalOnWinningTile", getU64Encoder()],
      ["winners", getU64Encoder()],
      ["remainingWinnerClaims", getU64Encoder()],
      ["numberOfPlayers", getU64Encoder()],
      ["settledMinerCount", getU64Encoder()],
      ["pendingSettlementBatchCallbacks", getU64Encoder()],
      ["rand", getOptionEncoder(getU64Encoder())],
      ["bonanzaPayoutAmount", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["directWinnerZincPayoutAmount", getU64Encoder()],
      ["closedMinerCount", getU64Encoder()],
      ["wildcatZincPayoutAmount", getU64Encoder()],
      ["isWildcatRound", getBooleanEncoder()],
      ["totalWildcatWeight", getU64Encoder()],
      ["wildcatTicket", getOptionEncoder(getU64Encoder())],
      ["wildcatWinnerMiner", getOptionEncoder(getAddressEncoder())],
      ["wildcatWinnerPlayer", getOptionEncoder(getAddressEncoder())],
      ["wildcatSelectionComplete", getBooleanEncoder()],
      ["wildcatClaimed", getBooleanEncoder()],
      ["blockhashEntropyAccumulator", fixEncoderSize(getBytesEncoder(), 32)],
      ["blockhashEntropyContributionCount", getU64Encoder()],
      ["wildcatEntries", getArrayEncoder(getRoundWildcatEntryRangeEncoder())],
    ]),
    (value) => ({ ...value, discriminator: ROUND_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Round} account data. */
export function getRoundDecoder(): Decoder<Round> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["status", getRoundStatusDecoder()],
    ["id", getU64Decoder()],
    ["initialized", getBooleanDecoder()],
    ["startSlot", getOptionDecoder(getU64Decoder())],
    ["endSlot", getOptionDecoder(getU64Decoder())],
    ["closeAfterSlot", getOptionDecoder(getU64Decoder())],
    ["totalDeployed", getU64Decoder()],
    ["buybackFeeLamports", getU64Decoder()],
    ["winningSquare", getOptionDecoder(getU64Decoder())],
    ["totalOnWinningTile", getU64Decoder()],
    ["winners", getU64Decoder()],
    ["remainingWinnerClaims", getU64Decoder()],
    ["numberOfPlayers", getU64Decoder()],
    ["settledMinerCount", getU64Decoder()],
    ["pendingSettlementBatchCallbacks", getU64Decoder()],
    ["rand", getOptionDecoder(getU64Decoder())],
    ["bonanzaPayoutAmount", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["directWinnerZincPayoutAmount", getU64Decoder()],
    ["closedMinerCount", getU64Decoder()],
    ["wildcatZincPayoutAmount", getU64Decoder()],
    ["isWildcatRound", getBooleanDecoder()],
    ["totalWildcatWeight", getU64Decoder()],
    ["wildcatTicket", getOptionDecoder(getU64Decoder())],
    ["wildcatWinnerMiner", getOptionDecoder(getAddressDecoder())],
    ["wildcatWinnerPlayer", getOptionDecoder(getAddressDecoder())],
    ["wildcatSelectionComplete", getBooleanDecoder()],
    ["wildcatClaimed", getBooleanDecoder()],
    ["blockhashEntropyAccumulator", fixDecoderSize(getBytesDecoder(), 32)],
    ["blockhashEntropyContributionCount", getU64Decoder()],
    ["wildcatEntries", getArrayDecoder(getRoundWildcatEntryRangeDecoder())],
  ]);
}

/** Gets the codec for {@link Round} account data. */
export function getRoundCodec(): Codec<RoundArgs, Round> {
  return combineCodec(getRoundEncoder(), getRoundDecoder());
}

export function decodeRound<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Round, TAddress>;
export function decodeRound<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Round, TAddress>;
export function decodeRound<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Round, TAddress> | MaybeAccount<Round, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRoundDecoder(),
  );
}

export async function fetchRound<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Round, TAddress>> {
  const maybeAccount = await fetchMaybeRound(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRound<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Round, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRound(maybeAccount);
}

export async function fetchAllRound(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Round>[]> {
  const maybeAccounts = await fetchAllMaybeRound(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRound(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Round>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRound(maybeAccount));
}
