/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ROUND_SECRET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  81, 230, 76, 166, 69, 99, 137, 216,
]);

export function getRoundSecretDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROUND_SECRET_DISCRIMINATOR
  );
}

export type RoundSecret = {
  discriminator: ReadonlyUint8Array;
  /** Round this encrypted value belongs to. */
  roundId: bigint;
  /** True once this PDA has been initialized for a specific round ID. */
  initialized: boolean;
  /** Rescue CTR nonce used by the encrypted payload. */
  nonce: Option<bigint>;
  /** Ciphertext limbs holding the encrypted round random value and bonus salt. */
  ciphertexts: Option<Array<ReadonlyUint8Array>>;
  /** PDA bump seed. */
  bump: number;
};

export type RoundSecretArgs = {
  /** Round this encrypted value belongs to. */
  roundId: number | bigint;
  /** True once this PDA has been initialized for a specific round ID. */
  initialized: boolean;
  /** Rescue CTR nonce used by the encrypted payload. */
  nonce: OptionOrNullable<number | bigint>;
  /** Ciphertext limbs holding the encrypted round random value and bonus salt. */
  ciphertexts: OptionOrNullable<Array<ReadonlyUint8Array>>;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link RoundSecretArgs} account data. */
export function getRoundSecretEncoder(): Encoder<RoundSecretArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU64Encoder()],
      ["initialized", getBooleanEncoder()],
      ["nonce", getOptionEncoder(getU128Encoder())],
      [
        "ciphertexts",
        getOptionEncoder(
          getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32), { size: 2 })
        ),
      ],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ROUND_SECRET_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link RoundSecret} account data. */
export function getRoundSecretDecoder(): Decoder<RoundSecret> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU64Decoder()],
    ["initialized", getBooleanDecoder()],
    ["nonce", getOptionDecoder(getU128Decoder())],
    [
      "ciphertexts",
      getOptionDecoder(
        getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32), { size: 2 })
      ),
    ],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RoundSecret} account data. */
export function getRoundSecretCodec(): Codec<RoundSecretArgs, RoundSecret> {
  return combineCodec(getRoundSecretEncoder(), getRoundSecretDecoder());
}

export function decodeRoundSecret<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<RoundSecret, TAddress>;
export function decodeRoundSecret<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<RoundSecret, TAddress>;
export function decodeRoundSecret<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<RoundSecret, TAddress> | MaybeAccount<RoundSecret, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRoundSecretDecoder()
  );
}

export async function fetchRoundSecret<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<RoundSecret, TAddress>> {
  const maybeAccount = await fetchMaybeRoundSecret(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRoundSecret<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<RoundSecret, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRoundSecret(maybeAccount);
}

export async function fetchAllRoundSecret(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<RoundSecret>[]> {
  const maybeAccounts = await fetchAllMaybeRoundSecret(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRoundSecret(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<RoundSecret>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRoundSecret(maybeAccount));
}
