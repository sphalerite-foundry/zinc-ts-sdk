/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  155, 12, 170, 224, 30, 250, 204, 130,
]);

export function getConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CONFIG_DISCRIMINATOR);
}

export type Config = {
  discriminator: ReadonlyUint8Array;
  /** Admin authorized to update config. */
  admin: Address;
  /** Crank authority authorized to execute permissioned maintenance instructions. */
  crank: Address;
  /** Treasury address. */
  treasury: Address;
  /** Total deploy-time SOL fee skim in basis points. */
  deployTotalFeeBps: bigint;
  /** Number of slots a round stays open once activated. */
  roundDurationSlots: bigint;
  /** Number of slots a stockpile stays open once activated. */
  stockpileDurationSlots: bigint;
  /** Minimum stockpile bricks required to join a cycle, in `x10k` units. */
  stockpileMinEntryBricksX10k: bigint;
  /** Gross deploy-time SOL admin fee in basis points. */
  deployAdminFeeBps: bigint;
  /** Gross deploy-time SOL stockpile fee in basis points. */
  deployStockpileFeeBps: bigint;
  /** Gross deploy-time SOL affiliate fee in basis points when the player is bound. */
  deployAffiliateFeeBps: bigint;
  /** Curve-minted ZINC admin skim in basis points. */
  curveAdminFeeBps: bigint;
  /** Share of post-skim curve mint reserved for direct winner payouts, in basis points. */
  winnerZincShareBps: bigint;
  /** Share of post-skim curve mint that accrues to the stockpile ZINC pot, in basis points. */
  stockpileZincShareBps: bigint;
  /** Minimum gross lamports required to enter a round. */
  minDeployLamports: bigint;
  /** Launch-time maximum mint for one round before ZINC factoring and support caps. */
  curveMaxRoundMint: bigint;
  /** SOL factoring lamports where the deploy curve reaches about `1 - e^-1` of `curve_max_round_mint`. */
  curveSaturationLamports: bigint;
  /** ZINC factoring amount that halves scarcity emissions, in base units. */
  curveHistoryMinted: bigint;
  /** Min target ZINC support lamports required for one emitted token. */
  curveTargetSupportLamportsPerZinc: bigint;
  /** Maximum cumulative ZINC the close-round mint path may mint, in base units. */
  curveMaxSupply: bigint;
  /** PDA bump seed. */
  bump: number;
  /** Whether affiliates may currently withdraw accrued SOL from their player profiles. */
  affiliateWithdrawalsEnabled: boolean;
  /** Flat deploy-time stockpile-bricks bonus for referred players, in `x10k` units. */
  deployAffiliateBonusBricksX10k: bigint;
  /** Deterministic Wildcat cadence; `0` disables Wildcat. */
  wildcatRoundFrequency: bigint;
  /** Direct-winner ZINC share reserved for Wildcat winners, in ppm. */
  wildcatWinnerZincSharePpm: bigint;
  /** Bonanza roll modulo divisor; `1` makes every winner-positive round eligible. */
  bonanzaHitDivisor: bigint;
  /** ZINC fee skim for round winner claims, in basis points. */
  roundClaimZincFeeBps: bigint;
  /** Minimum ZINC fee required to enter one stockpile cycle, in mint base units. */
  stockpileEntryMinZincFee: bigint;
  /** Stockpile entry fee as a share of the live stockpile ZINC pot, in basis points. */
  stockpileEntryPotFeeBps: bigint;
  /** Accepted-entry step multiplier for stockpile entry costs, in basis points. */
  stockpileEntryStepBps: bigint;
  /** Staking brick issuance rate per claimed ZINC, in `x10k` units. */
  stakingBricksPerZincX10k: bigint;
  /** Number of slots over which newly melted staking rewards vest. */
  stakingRewardVestingSlots: bigint;
  /** Number of slots after reveal before selecting the next playable round. */
  roundStartDelaySlots: bigint;
};

export type ConfigArgs = {
  /** Admin authorized to update config. */
  admin: Address;
  /** Crank authority authorized to execute permissioned maintenance instructions. */
  crank: Address;
  /** Treasury address. */
  treasury: Address;
  /** Total deploy-time SOL fee skim in basis points. */
  deployTotalFeeBps: number | bigint;
  /** Number of slots a round stays open once activated. */
  roundDurationSlots: number | bigint;
  /** Number of slots a stockpile stays open once activated. */
  stockpileDurationSlots: number | bigint;
  /** Minimum stockpile bricks required to join a cycle, in `x10k` units. */
  stockpileMinEntryBricksX10k: number | bigint;
  /** Gross deploy-time SOL admin fee in basis points. */
  deployAdminFeeBps: number | bigint;
  /** Gross deploy-time SOL stockpile fee in basis points. */
  deployStockpileFeeBps: number | bigint;
  /** Gross deploy-time SOL affiliate fee in basis points when the player is bound. */
  deployAffiliateFeeBps: number | bigint;
  /** Curve-minted ZINC admin skim in basis points. */
  curveAdminFeeBps: number | bigint;
  /** Share of post-skim curve mint reserved for direct winner payouts, in basis points. */
  winnerZincShareBps: number | bigint;
  /** Share of post-skim curve mint that accrues to the stockpile ZINC pot, in basis points. */
  stockpileZincShareBps: number | bigint;
  /** Minimum gross lamports required to enter a round. */
  minDeployLamports: number | bigint;
  /** Launch-time maximum mint for one round before ZINC factoring and support caps. */
  curveMaxRoundMint: number | bigint;
  /** SOL factoring lamports where the deploy curve reaches about `1 - e^-1` of `curve_max_round_mint`. */
  curveSaturationLamports: number | bigint;
  /** ZINC factoring amount that halves scarcity emissions, in base units. */
  curveHistoryMinted: number | bigint;
  /** Min target ZINC support lamports required for one emitted token. */
  curveTargetSupportLamportsPerZinc: number | bigint;
  /** Maximum cumulative ZINC the close-round mint path may mint, in base units. */
  curveMaxSupply: number | bigint;
  /** PDA bump seed. */
  bump: number;
  /** Whether affiliates may currently withdraw accrued SOL from their player profiles. */
  affiliateWithdrawalsEnabled: boolean;
  /** Flat deploy-time stockpile-bricks bonus for referred players, in `x10k` units. */
  deployAffiliateBonusBricksX10k: number | bigint;
  /** Deterministic Wildcat cadence; `0` disables Wildcat. */
  wildcatRoundFrequency: number | bigint;
  /** Direct-winner ZINC share reserved for Wildcat winners, in ppm. */
  wildcatWinnerZincSharePpm: number | bigint;
  /** Bonanza roll modulo divisor; `1` makes every winner-positive round eligible. */
  bonanzaHitDivisor: number | bigint;
  /** ZINC fee skim for round winner claims, in basis points. */
  roundClaimZincFeeBps: number | bigint;
  /** Minimum ZINC fee required to enter one stockpile cycle, in mint base units. */
  stockpileEntryMinZincFee: number | bigint;
  /** Stockpile entry fee as a share of the live stockpile ZINC pot, in basis points. */
  stockpileEntryPotFeeBps: number | bigint;
  /** Accepted-entry step multiplier for stockpile entry costs, in basis points. */
  stockpileEntryStepBps: number | bigint;
  /** Staking brick issuance rate per claimed ZINC, in `x10k` units. */
  stakingBricksPerZincX10k: number | bigint;
  /** Number of slots over which newly melted staking rewards vest. */
  stakingRewardVestingSlots: number | bigint;
  /** Number of slots after reveal before selecting the next playable round. */
  roundStartDelaySlots: number | bigint;
};

/** Gets the encoder for {@link ConfigArgs} account data. */
export function getConfigEncoder(): FixedSizeEncoder<ConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["crank", getAddressEncoder()],
      ["treasury", getAddressEncoder()],
      ["deployTotalFeeBps", getU64Encoder()],
      ["roundDurationSlots", getU64Encoder()],
      ["stockpileDurationSlots", getU64Encoder()],
      ["stockpileMinEntryBricksX10k", getU64Encoder()],
      ["deployAdminFeeBps", getU64Encoder()],
      ["deployStockpileFeeBps", getU64Encoder()],
      ["deployAffiliateFeeBps", getU64Encoder()],
      ["curveAdminFeeBps", getU64Encoder()],
      ["winnerZincShareBps", getU64Encoder()],
      ["stockpileZincShareBps", getU64Encoder()],
      ["minDeployLamports", getU64Encoder()],
      ["curveMaxRoundMint", getU64Encoder()],
      ["curveSaturationLamports", getU64Encoder()],
      ["curveHistoryMinted", getU64Encoder()],
      ["curveTargetSupportLamportsPerZinc", getU64Encoder()],
      ["curveMaxSupply", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["affiliateWithdrawalsEnabled", getBooleanEncoder()],
      ["deployAffiliateBonusBricksX10k", getU64Encoder()],
      ["wildcatRoundFrequency", getU64Encoder()],
      ["wildcatWinnerZincSharePpm", getU64Encoder()],
      ["bonanzaHitDivisor", getU64Encoder()],
      ["roundClaimZincFeeBps", getU64Encoder()],
      ["stockpileEntryMinZincFee", getU64Encoder()],
      ["stockpileEntryPotFeeBps", getU64Encoder()],
      ["stockpileEntryStepBps", getU64Encoder()],
      ["stakingBricksPerZincX10k", getU64Encoder()],
      ["stakingRewardVestingSlots", getU64Encoder()],
      ["roundStartDelaySlots", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Config} account data. */
export function getConfigDecoder(): FixedSizeDecoder<Config> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["crank", getAddressDecoder()],
    ["treasury", getAddressDecoder()],
    ["deployTotalFeeBps", getU64Decoder()],
    ["roundDurationSlots", getU64Decoder()],
    ["stockpileDurationSlots", getU64Decoder()],
    ["stockpileMinEntryBricksX10k", getU64Decoder()],
    ["deployAdminFeeBps", getU64Decoder()],
    ["deployStockpileFeeBps", getU64Decoder()],
    ["deployAffiliateFeeBps", getU64Decoder()],
    ["curveAdminFeeBps", getU64Decoder()],
    ["winnerZincShareBps", getU64Decoder()],
    ["stockpileZincShareBps", getU64Decoder()],
    ["minDeployLamports", getU64Decoder()],
    ["curveMaxRoundMint", getU64Decoder()],
    ["curveSaturationLamports", getU64Decoder()],
    ["curveHistoryMinted", getU64Decoder()],
    ["curveTargetSupportLamportsPerZinc", getU64Decoder()],
    ["curveMaxSupply", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["affiliateWithdrawalsEnabled", getBooleanDecoder()],
    ["deployAffiliateBonusBricksX10k", getU64Decoder()],
    ["wildcatRoundFrequency", getU64Decoder()],
    ["wildcatWinnerZincSharePpm", getU64Decoder()],
    ["bonanzaHitDivisor", getU64Decoder()],
    ["roundClaimZincFeeBps", getU64Decoder()],
    ["stockpileEntryMinZincFee", getU64Decoder()],
    ["stockpileEntryPotFeeBps", getU64Decoder()],
    ["stockpileEntryStepBps", getU64Decoder()],
    ["stakingBricksPerZincX10k", getU64Decoder()],
    ["stakingRewardVestingSlots", getU64Decoder()],
    ["roundStartDelaySlots", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link Config} account data. */
export function getConfigCodec(): FixedSizeCodec<ConfigArgs, Config> {
  return combineCodec(getConfigEncoder(), getConfigDecoder());
}

export function decodeConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Config, TAddress>;
export function decodeConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Config, TAddress>;
export function decodeConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Config, TAddress> | MaybeAccount<Config, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getConfigDecoder(),
  );
}

export async function fetchConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Config, TAddress>> {
  const maybeAccount = await fetchMaybeConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Config, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeConfig(maybeAccount);
}

export async function fetchAllConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Config>[]> {
  const maybeAccounts = await fetchAllMaybeConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Config>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeConfig(maybeAccount));
}

export function getConfigSize(): number {
  return 322;
}
