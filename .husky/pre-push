branch="$(git symbolic-ref --quiet --short HEAD || true)"
if [ "${branch}" = "master" ]; then
  echo "Error: direct commits and pushes on master are blocked." >&2
  echo "Create a feature branch and merge through a pull request." >&2
  exit 1
fi

npm run hooks:pre-push
